# Fid 核查报告 —— P1-S2-R4-C架构讨论 第 2 段（第 3–7 步）

**核查范围**：记录 `docs/01-需求摸底/S02-演示蓝图/R04-架构讨论/P1-S2-R4-C架构讨论.md` 第 3 步至第 7 步，及「⚠ 讨论中发现」节的发现三、四、五、六。
**原始对话**：`C:\Users\Philix\.claude\projects\d--ERX-001\3728d94b-256a-4852-a954-b93f5f0cce04.jsonl`，L330–L640。
**依据**：讨论契约 §2 纪律 1/3、§3.2、§6.1。**已按 §6.1 硬约束执行**——总结性概括不报，只报数值/技术细节/约束条件/否定表述/非常规命名的丢失或改写、原意强度与范围的改变、整条漏记。

---

## 一、轮数 / 步数对照表（重点核项 3）

本段内 transcript 的**真实用户发言**只有 3 条（L623「从决策5起」已属下一段、开第 8 步）：

| transcript 行 | 类型 | 内容要旨 | 记录落到哪一步 |
|---|---|---|---|
| L381 | **子 Agent 回报**（`<agent-message from="aa3d41256b0e5bc68">`，V-15） | V-15 判 `go` | **第 3 步**（新开一步） |
| L448 | **子 Agent 回报**（`<agent-message from="a34b871d693429e73">`，V-16） | V-16 判 `go` | **第 4 步**（新开一步） |
| L493 | 用户 | `决策 1、A` | —（被打断后重发，见下） |
| L496 | 系统 | `[Request interrupted by user]` | — |
| L499 | **用户** | `决策 1、A。决策 2、A。` | **第 5 步** |
| L556 | **用户** | `A`（决策 3） | **第 6 步** |
| L595 | **用户** | 三项编号消息（译名前提） | **第 7 步**（裁决回填在第 6 步②） |
| L623 | 用户 | `从决策5起` | 第 8 步（属第 3 段） |

**结论：对不上。本段 5 个步骤（3–7）只对应 3 个真实用户轮。** 第 3、第 4 步各自对应的是一条子 Agent 回报，不是用户消息。

**那次打断怎么处理的**：**合并成一步，处理正确，但记录全文未提这次打断。** L493 的 `决策 1、A` 是 L499 `决策 1、A。决策 2、A。` 的真子集，合并后无任何内容丢失，也没有因此多记或少记步骤。契约 §7 合法例外第 1 项（本轮被打断）成立。记录里 `grep 打断|中断|interrupt` 零命中。

---

## 二、逐条发现

### 问题 1（最要紧）：第 3、4 步为子 Agent 回报各开一步，违「一轮一步」，且与本记录第 2 步④自立的先例自相矛盾

- **记录怎么写的**：第 3 步标题「V-15 回报（导航记录不被 migrate 覆盖）」、第 4 步标题「V-16 回报（desk js 可 app 侧覆盖）」，均为独立步骤，两步的 `用户回答：` 格填 `—（探针回报，无待答项）`。
- **原始对话实际是什么**：L381、L448 都是 `<agent-message from="...">` 开头的子 Agent hand-back，harness 在其后还附了明文提示「this was not typed by your user」。本段真实用户轮只有 L499、L556、L595 三条。
  更关键的是：**同一份记录的第 2 步④处理的是同一类事件（V-14 探针回报），却明文写「按规范 §7.1，标 `附属=新Session做` 的可选步骤不另占 Round，故本次回报记在本步内、不新增步号」**。即记录自己已经立过正确做法，第 3、4 步却反着做了。
- **属哪类问题**：**违 §2 纪律 1（一轮一步、步数即用户消息数），记录内部自相矛盾。** 后果是本卷此后全部步号相对用户消息数偏移 2 位，「步数即用户消息数」这条使边界可机械核验的性质在本卷失效。
- **补充（可能的机械成因，供你判断处置）**：子 Agent hand-back 在 transcript 里 `type` 为 `user`，落档开关的 `UserPromptSubmit` 很可能把它计成了一个对话轮、进而要求最大步号 +1。若属此因，问题在 hook 的轮边界判定与契约 §2 纪律 1 不一致，而非单纯的执行者笔误。

### 问题 2：第 5 步① 把 Claude 自称"不是判断"的偏好写成用户"定案"

- **记录怎么写的**：第 5 步① `用户回答：决策 1 选 A（单 app）、决策 2 选 A（自有 app 末位）`；`结论：两条定案，回填至第 2 步②③（含 app 名 erx_core 与「不用 override_doctype_class」的硬约束）`。
- **原始对话实际是什么**：用户 L499 全文只有 `决策 1、A。决策 2、A。`。而第 2 步② 里 Claude 自己写的是：「Claude 倾向 `erx_core`……**但这是可改的偏好、不是判断**」；`override_doctype_class` 那条则是 Claude 的「附带一条硬约束**提议**」。用户对这两项一字未表态。
- **属哪类问题**：**改变原意的范围**（用户裁决的是选项 A，记录把 Claude 明确排除在裁决范围外的偏好、以及一条未经询问的提议，一并升格为「定案」）。`用户回答` 格本身没写错，问题在 `结论` 格把裁决面放宽了。

### 问题 3：用户 L595 的自带编号被重排，此后两处「用户第③条」指错

- **记录怎么写的**：第 6 步② `用户回答：推翻了 A 案的前提，给出三条：① 客户不会改译名…；② "现场可改"只是开发阶段方便改动直接看到效果，并不强求；③ 译名稳定后最终会写成更稳定的内置形式。并追问：在这种情况下依然选 A 更好吗。` 第 7 步③ 标题「一条附带的收益（**用户第③条**的直接后果）」，第 7 步理由 3 亦写「用户**第③条**定了终态是内置形式」。
- **原始对话实际是什么**（L595 全文）：
  `1、客户不会改译名，译名只会由客户上报有问题的译名后，由开发者改。2、之所以要现场可改，只是为了在开发阶段方便改动直接看到效果，实际上并不强求，而且在译名稳定后，最终还是会写成更稳定的内置的形式。3、在这种情况下，依然选A更好吗。`
  即：**用户的第 2 项包含两个半句**（现场可改不强求 + 终态写成更稳定的内置形式），**用户的第 3 项是那个追问**。记录把用户第 2 项拆成自己的 ②③，把用户第 3 项降级为尾随的「并追问」。
- **属哪类问题**：**违 §3.2「用户消息常自带编号，议题即按用户的编号与切分来分」；并派生归属编号错**——第 7 步两处交叉引用「用户第③条」，回查原文会落到那个追问上，而不是「终态是内置形式」那句。
- **内容层面已核无问题**：三项的信息全部在记录里，**强度未被改写**——用户的「实际上并不强求」逐字保留为「并不强求」，没有写成「不需要」；「最终还是会写成更稳定的内置的形式」保留为「最终会写成更稳定的内置形式」，没有写成"已定"或"必须"。追问也被答了（第 7 步开篇即「改判为 B」，对应 assistant L619 原话「不是 —— 改选 B（全走 csv）」）。

### 问题 4：两份探针回报都请求"是否提交由你裁决"，Session 未转呈、记录未落记

- **记录怎么写的**：第 3、4 步的「留档」段只列文件名与「未删」，全文无提交/入库待裁决的字样；遗留问题表也没有这一条。
- **原始对话实际是什么**：V-15 回报末尾：「**未提交任何东西**（按 CLAUDE.md 版本管理规则）。上述文件目前是 untracked，要不要 commit 请你定。」V-16 回报末尾：「**按项目的版本管理规则，我没有做任何提交** —— 上述文件与待验表改动均为未跟踪/未提交状态，是否入库请你裁决。」我扫了 L430–L500 全部 assistant 文本，**没有任何一条把这个裁决请求转呈给用户**。
- **属哪类问题**：**整条漏记**（一条明确指名要用户裁决的开放项，既未进任何步，也未进遗留问题表）。

### 问题 5：「前两条是本步新查的机制事实」——其中理由 2 的依据不是本步查的

- **记录怎么写的**：第 7 步「⭐ Claude 改判为 B，四条理由（**前两条是本步新查的机制事实，不是重新权衡同一批依据**）」。
- **原始对话实际是什么**：
  - 理由 1（csv 不支持注释与第四列）——本步的 L600 确实新跑了 `sed -n '200,212p' frappe/translate.py`，结果在 L605，`elif item:` 分支与 key 构造都在其中。**这一条成立。**
  - 理由 2（csv 与 `Translation` 的 key 格式逐字相同）——csv 侧那一半来自本步 L605；但 `get_user_translations` 的 key 构造（`translate.py:221-234`）是在**早得多的一轮**读的：工具调用 L194、结果 L195（`grep -n "def get_user_translations" -A 14`）。本段（L330 之后）没有再读它。
- **属哪类问题**：**把早先所查写成本步新查**。范围小、不影响结论成立（两处读取都真实存在），但「不是重新权衡同一批依据」这句对理由 2 只成立一半。

### 问题 6：发现四把一条注释的行号归错

- **记录怎么写的**：发现四「`frappe/public/js/frappe/ui/menu.js:96-97` 与 `frappe/public/js/frappe/ui/settings_dialog.js:255-258` 都读 `item.icon_html`（**后者还带注释说明它接受 raw trusted markup**）」。
- **原始对话实际是什么**（L459 的 grep，结果 L464）：那条 raw/TRUSTED 注释在 `settings_dialog.js:144`（`* // icon_html: "<svg…>" // alternative: raw, TRUSTED markup only`）；落在 `:255-258` 区间内的注释是 `:257 // Never populate icon_html from a server response or user input.`
- **属哪类问题**：**技术细节归位错**（行号）。实质结论（两个文件都读 `icon_html`、故「零读取点」不成立）不受影响。

### 问题 7：发现五的交叉引用指错

- **记录怎么写的**：发现五「机制成立（**发现四**已证可覆盖）」。
- **原始对话实际是什么**：「可覆盖」是 V-16 第 2 档实测（四手法 17/2/26/1 次 + 对照组）所证，记在第 4 步②③。发现四讲的是 V-12「零读取点」判据不成立，与"能否覆盖"无关。
- **属哪类问题**：**交叉引用指错**（低）。

### 问题 8：V-15 回报里一条 migrate 期间的删除动作未落记

- **记录怎么写的**：第 3 步⑤「安全收尾核验」表未提。
- **原始对话实际是什么**（L381）：「一处非本探针造成的附带现象：第一次 migrate 日志有 `Deleting icon Frappe Framework`，来自 `delete_duplicate_icons()` 清理重命名后的陈旧 App 图标，与三条测试记录无关，图标总数仍 35。」
- **属哪类问题**：**技术细节丢失**（低）。净结果（图标总数 35）已通过「记录数与基线同（32 / 573 / 35）」保留，但「migrate 会顺带删陈旧 App 图标」这一机制事实对决策 9 的「改记录」手法有参考价值，已丢。
- **同类另一处（更轻）**：探针「避开了命名字段（`title`/`label` 是 `autoname: field:*`）以免记录被重命名触发 orphan 清理」这条方法论约束未落记。

### 问题 9：记录未记那次打断

- 见上文对照表。**合并本身正确、无内容丢失**，仅记录全文未提该打断。列此仅供你判断是否需要（§7 例外第 1 项是给 hook 用的，不必然要求写进记录）。

---

## 三、已核、无问题（覆盖面交代）

**用户发言真实性（重点核项 4）——全部通过，无一处编造：**
- 第 5 步① `用户回答` = L499 逐项对应。**已核，无问题。**
- 第 6 步① `用户回答：选 A（按语义分派）` = L556 的 `A`。**已核，无问题**（用户只说了一个字母，记录未替用户加理由）。
- 第 6 步② `用户回答` 三项 = L595，强度与否定表述均未改写（见问题 3 的内容层结论）。**已核，无问题。**
- 第 7 步 `用户回答：—（本步是 Claude 对用户三条前提的改判回应，用户裁决见第 6 步②）`。**已核，如实标注、未借用户之口。**
- 第 3、4 步 `用户回答：—（探针回报，无待答项）`。**已核，如实。**

**那句自我归错（重点核项 1）——成立，不是失真：**
记录写「Claude 推荐 A 时押的"演示现场发现译错能当场改"是**自己补的前提、用户从未提过**」。我在 `docs/` 全域搜 `当场改|现场可改|现场改`（排除 R04 本身）**零命中**；需求图谱 CR-002 只写交付形态是「csv 批量基线 + `Translation` 记录做运行时最高优先覆盖」，IM-002 是抽检、不是现场可改；该说法在 transcript 里首次出现于 Claude 自己的第 6 步选项表（assistant L583）。**已核，自我归错成立。**

**改判四条理由的源码取证（重点核项 1 第三问）**：理由 1 有本步实读（L600→L605），理由 2 的两半分别有实读（L605 与早先的 L194→L195）。**均非凭空推断**；唯"本步新查"这一限定对理由 2 不准，已列为问题 5。

**行号抽查（重点核项 5）——抽 10 条，全部在原始对话里有对应读取动作与输出，且我另比对了 live 源码：**

| 记录引用 | 对应工具调用→结果 | 核验 |
|---|---|---|
| `import_file.py:129-137` / `:140-141` / `:124-126` | L385→L390（`sed -n '120,150p'`） | 成立。实际 124-126 即 `is_db_timestamp_latest` 表达式；141 是那个 `if`（`continue` 落 142，记录写 140-141，判据定位无误） |
| `frappe/model/sync.py:120-126` | L385→L390（`grep app_level_folders -B3 -A8`） | 成立，逐字一致 |
| `modules/utils.py:125-200`（增改不删） | L188→L189 + L527→L532 + L535→L536（含"有无 delete"的专项核验） | 成立；「唯一例外是 `Custom DocPerm` 整表删重建」亦有实证 |
| `data_import.py:361-362`（`force=True`） | L504→L505 + L535→L536 | 成立，逐字一致 |
| `translate.py:190-193`（每 app 每语言一个 csv） | L561→L566 | 成立 |
| `translate.py:196-219` / `:206-208` | L600→L605（另有更早的 L173→L174 覆盖整函数） | 成立 |
| `translate.py:155-158`（`Translation` 在全部 app 之后 update） | L173→L174 | 成立 |
| `translation.py:28-29`（`validate` 跑 `sanitize_html`）／`:31-37`（`on_update` 双 hdel） | L561→L566 + L571→L572 | 成立，逐字一致 |
| `html_utils.py:158-163`（is_json / BeautifulSoup 双短路） | L579→L580 | 成立 |
| `frappe/hooks.py:25-33` + `frappe/__init__.py:916-918` | L452→L453 | 成立，逐字一致 |

**未核项交代**：发现三引用 `开发守则.md`「源码改动须登记技术债」一句，按本次分派约束（不读常驻文件）**未核**。记录里 `boot.py:442-457` 等引用落在第 12 步及之后，不在本段。

**子 Agent 回报转写忠实度（重点核项 6）——整体忠实，且有两处正向更正：**
- V-15 三表实测数据（字段、json 原值、探针值、`db_modified` 一字未变、子行名 `d25tkm7tnm`、"整条导入被跳过"而非"重新导入写回同值"）**逐项一致**。表中 `Workspace Sidebar Item` 的「migrate 后」记录写「保持」、原报告写 `Stock Entry-V15TEST`——**等价，无失真**（原值与探针值同行已给出）。
- V-15 的失效边界（json `modified` 调到未来 → 三表全部打回 json 原值；子行 name `d25tkm7tnm`→`9h1c109pd0`；按子行 name 的外部引用会断）**完整保留**，且「改动只维持到上游升级换掉该 json 为止」这条限定被写进第 3 步结论与决策 9 的 ADR 要求，**未被放大成"永久有效"**。
- **⭐ 记录更正了 V-16 的一处数字且更正正确**：V-16 报告说「desk 的 **9** 个 bundle」并引 `hooks.py:25-34`；记录写 **8** 个并引 `:25-33`，逐条列出 `libs/billing/desk/list/form/controls/report/telemetry`。我读 live 源码确认是 8 条（26-33 行），且与"11 个 script 标签 = 8 frappe + 1 erpnext + 2 自有 app"、"卸载后回到 9 个"自洽。**记录是对的，原报告是错的。**
- **⭐ 记录补强了 V-15 的一处"唯一调用点"**：报告说 `create_desktop_icons_from_workspace()` 全仓只有 `install.py:203` 一个调用点；记录另列出 `frappe/commands/site.py:1602` 的 `bench sync-desktop-icons` 命令（Claude 自己 grep 所得，L422→L423 有实证），**没有照抄那个偏强的"唯一"**。
- **V-16 自己声明"没验到"的三条，记录如实保留、一条未缩**：① 没验 frappe 升级后仍生效（依赖类名/方法名/签名这些上游非公开 API，改名即静默失效；「本命题只问能不能覆盖，未问能撑多久」）；② 没验并发/多标签/跨浏览器；③ 没验生产模式（`developer_mode=0` + gunicorn/nginx）与裸路径无哈希时的缓存边界。第 4 步结论还明写「`go` 的范围须按未触及边界读」并把"升级复验"定为明示义务。**已核，无缩小。**
- V-16 的 `go` 条件、对照组、四手法调用次数 17/2/26/1、build 前后一致、bundle 路径 build 前 404 / 后 200、裸路径前后 200、assets.json 44 条逐条 diff（新增 1、零删除、其余 44 条哈希全变、`desk.bundle` `LHZJYI3U`→`OSOIKW5M`）——**逐项一致**。（「44 条逐条 diff 又说其余 44 条全变、总数 45→44」这处张力**源自原报告本身**，记录忠实照转，不算记录失真。）
- V-16 收尾项：`.pth` 与 `sites/assets/erx_spike` 符号链接一并删、`uninstall-app` 不管的 assets.json 条目 45→44 且"它会被塞进每个 desk 页的 boot 负载"、`apps.txt` 逐字节回到 `frappe\nerpnext`（原文件结尾无换行）、数据库 7 类表无残留、`bench serve` PID 5109 与端口 8000、并发同站点风险与"并行 spike 应错开或显式串行化"的教训——**逐项落记**。上游源码状态一处处理得当：报告说 erpnext 有 2 文件显示改动（mtime 09-18、纯行尾符差异），记录写「Claude 复核时已为空」并**如实交代了这一出入**（L470→L471 有实证，两仓 porcelain 均空）。

**发现三、四、五、六的事实层核验：**
- **发现三**：四条件守卫 `self.app and self.standard and not frappe.flags.in_import and frappe.conf.developer_mode` 逐字正确（L401→L402、L427→L432）；`on_trash` 删 json、`after_rename` 删旧建新正确；`desktop_icon.py` 守卫同构正确；`site_config.json:6` 的 `"developer_mode": 1` 有实证（L397→L398）。"差点造成假阳性"的机制与"两边一样、无从覆盖"的表述与原报告一致。**已核，无问题。**
- **发现四**：`views/workspace/sidebar_header.js` 路径不存在（`ls` 报 No such file）、实为 `ui/sidebar/sidebar_header.js`、行号 145/160 对得上——**全部有实证**（L459→L464）。「另有 `select.js`／`tree.js` 里的同名局部变量，与本议题无关」与 grep 输出逐条吻合。「成立的只是缩小版：`add_app_item` 那一处只读 `icon`/`icon_url`」与原报告「背景说的这一处是对的」一致。"推翻 Stage 概况第 17 条"有实证（L470→L471 命中第 17 条原文）。「LG-021 第五次应验」与 `P1-概况.md`（已应验三次 + 第四次）计数衔接得上。**已核，除问题 6 的行号归位外无问题。**
- **发现五**：判「无法判定」、不影响主命题；两个被排除的候选及其实证（换掉方法仍 17 次调用而 404 仍 1 次、未打补丁对照组也是 1 次；卸载后 `app_data` 回到 2 条、404 依旧）；指向 `set_header_icon()`（`sidebar_header.js:299-316`，四分支都拼 `` `<img src=${...}>` `` 且属性值未加引号）但 `app_data[0].app_logo_url` 实测有值、DOM 最终正常 svg ⇒ 404 在中间态被重渲染覆盖；CDP `initiator.type="script"` 而 stack 为空——**与原报告逐项一致**。「故'修 LG-074 只要三行'这个判断本次给不出」「不可写成已解决」这两处否定表述**完整保留**。**已核，除问题 7 的交叉引用外无问题。**
- **发现六**：工作量表第 6 项原文「洗 169 行 HTML 实体污染（`get_translation_dict_from_file` 不做反转义）」**与 `P1-S2-R2-A侵入度与工作量.md` 第 48 行逐字一致**；`read_csv_file`（`translate.py:666-676`）只做 `csv.reader` 逐格取值、`get_translation_dict_from_file` 只做 `\n` 还原与 `strip`——有实证（L579→L580、L173→L174）；`sanitize_html` 双短路（`is_json` 原样返回、`BeautifulSoup(...).find()` 为假则原样返回、只有真含标签才走 nh3）——有实证。两条推论均标为 Claude 所推，且推论 2 自带范围限定「本项目译名以纯文本为主、影响面小」，**未夸大**。**已核，无问题。**

**其它数值抽核（全部通过）**：第 6 步② 背景的 1776 / 3037 / Raven 1867 占 61% 与需求图谱 CR-002 逐字一致；914 组撞名与工作量表一致；4813 = 1776 + 3037 算术正确；第 7 步④ 引图谱 CR-002「`zh.csv` 做批量基线 + `Translation` 记录做运行时最高优先覆盖」为逐字引用。第 5 步② 的装载点 `migrate.py:171 sync_fixtures()` / `:180 sync_customizations()` / `utils/fixtures.py:26` 均有实证（L300→L301、L310→L311）。第 3 步⑤ 的备份四件齐与旧三份未被覆盖、`保留-R6重装前/` 未动，均有实证（L412、L418）。第 3 步留档段关于 `V15-survey.py` 归属 V-15 的澄清，与 transcript L354/L366/L375 的实际澄清过程一致（⚠ 该文件现已不在 `Spike/` 下，但那是本段之后的事，不属本段核查范围）。
