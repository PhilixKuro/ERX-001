# 记录忠实度核查 · 第 4 段（第 13–15 步）

**核查对象**：`docs/01-需求摸底/S02-演示蓝图/R04-架构讨论/P1-S2-R4-C架构讨论.md`
**对照原始对话**：`C:\Users\Philix\.claude\projects\d--ERX-001\3728d94b-256a-4852-a954-b93f5f0cce04.jsonl`，L950–L1162（文件末行 L1162）
**依据**：讨论契约 §6.1（总结性记录不算失真；硬约束只有「数值/技术细节/约束/否定表述/非常规命名不可丢失或改写」与「不得改变原意的强度与范围」）
**日期**：2026-09-25

---

## 一、轮数 / 步数对照表

| # | transcript 行 | 该轮的输入 | 输入性质 | 落成哪一步 |
|---|---|---|---|---|
| 1 | L955 | V-18 探针回报（`<agent-message from="a5fa52141a1312fd5">`） | **子 Agent 回报，非用户发言** | 第 13 步 |
| 2 | L1020 | `三条均按你的建议` | 真实用户发言 | 第 14 步（并回填第 12、13 步的 `用户回答`） |
| 3 | L1069 | `A` | 真实用户发言 | 第 15 步（并回填第 14 步⑤的 `用户回答`） |
| 4 | L1116 | `均同意。` | 真实用户发言 | **本轮在跑（C 步收尾 + §6 验证），尚未落步** |

**结论：轮数与步数对得上。** 本段 3 个已完成对话轮 ⇒ 第 13、14、15 步，一轮一步、无注水无并合。第 4 轮（`均同意。`）是当前在跑的这一轮，第 16 步尚未写，记录第 15 步的 `用户回答` 仍为「待用户回答」、`结论` 为「待裁决」——**这是正确的真实快照，不是漏记**。

> 注：第 1 轮的输入是子 Agent 回报而非用户发言。记录第 13 步的 `提出者` 写「Claude（派出的探针回报）」、`用户回答` 原为「待用户回答」（后由第 2 轮回填）——**归属处理正确**，没有把探针的话记成用户的话。

---

## 二、逐条核查

### A. 查出的问题（按严重度排序）

#### A-1【漏记 · 否定表述丢失 · 本段最要紧】探针明说「A 案未经实测」，记录第 13 步全文无痕

- **记录怎么写的**：第 13 步⑤写「**A 案同时是 zelin 的形态，且现在知道它为何必须如此**」，②写 zelin 三钩子「**是当前唯一能让自制科目表建出来的走法**」，结论写「**决策 5 定案 A**」。全段（L752–L1058）grep `A 案`，只有 4 处，**没有任何一处记「A 案本次未经实测」**。
- **原始对话实际是什么**：L955 回报「我没验到的」节第 2 条原文：「**A 案（zelin 原样三钩子）未经实测** —— 命题只问 B，`erpnext_china_create_charts` 与三个 doc_events **刻意没抄进测试 app**」。同内容在 `C待验表.md` 第 232 行亦有（「即『A 案跑得通』本次**未经实测**，只是 zelin 上游在用」）。
- **属于哪类问题**：**漏记（否定表述丢失）**。§6.1 第一条明列「否定表述不可丢失」。
- **为什么要紧**：第 13 步正是改判 A 的那一步，记录同时把「A 案唯一可行」写成已知事实。读者（下一步是 D 步 Spec 消费本记录）会得到「A 已验」的印象，而实测覆盖面是「B 已证不通 + A 未测」。该否定表述在 `C待验表` 里还在，故未从项目里丢失，但**讨论记录这一侧丢了**。

#### A-2【漏记 · 对站点的实际改动】探针顺带删掉的 4 条历史孤儿数据，记录里没有

- **记录怎么写的**：第 13 步无「清理与环境核验」小节（对照第 3、4 步都有「⑤ 清理与环境核验」）。全记录 grep `孤儿`／`ERXD`／`China Tax`／`20260925` —— 关于这 4 条删除**零命中**（801 行那处「孤儿父引用 0」是科目树完整性指标，是另一件事）。
- **原始对话实际是什么**：L955 回报单列一节「**一处需登记的事**」：清理孤儿子行时顺带删除了 `China Tax - ERX`、`China Tax - ERXD` 的销项／进项子行各 2 条（共 4 条），成因是 `Company.on_trash` 用裸 SQL 删父表不级联子表；这两家公司在本 Session 之前已被删。另外 Claude 在 L1012 对用户报了「站点也已核干净（1 公司 / 95 科目 / `apps.txt` 原样 / 备份 `20260925_014739` 在）」——这段核验结论记录里也没有。
- **属于哪类问题**：**漏记**（对站点的实际改动 + Claude 已向用户报出的核验结论）。
- **缓解**：4 条删除如实登记在 `C待验表.md` 第 255 行的复核建议节。故不是项目级丢失，是记录级漏记。

#### A-3【交叉引用错】发现十四把崩点归到「发现十一」

- **记录怎么写的**：发现十四末句「**且它与发现十一的崩点同型**——`financial_report_template.py:144` 正是 `coa.get(...)` 不做 None/类型检查」。
- **实际**：发现十一讲的是原生塞进来的无编号 17% `VAT` 科目，**里面没有崩点**。`financial_report_template.py:144` 的崩点写在第 13 步①。
- **属于哪类问题**：内部交叉引用悬空/指错（§6.3 所列自洽性缺陷）。技术事实本身正确（144 行确实是 `coa.get(...)`，已核 L972、L991），错的只是指向。

#### A-4【引号内插字】第 14 步引第 12 步原文时加了「同名」二字

- **记录怎么写的**：第 14 步②「第 12 步写 D『需另建 `Module Def` 与**同名** `Workspace`（机制 1 表明前端按 `workspace_title` 取，故须有同名 workspace 承载）』」。
- **第 12 步原文**（记录第 721 行）：「需另建 `Module Def` 与 `Workspace`（机制 1 表明前端按 `workspace_title` 取，故须有同名 workspace 承载）」——**没有「同名」这两个字**（同名只出现在括号里）。
- **属于哪类问题**：以「」形式呈现的引文非逐字。**语义未变**（括号里本就说「须有同名 workspace 承载」），故不影响那处「更正」的成立；仅记录纪律上的瑕疵。

#### A-5【行号范围偏一行】`company.py:418-423`

- **记录怎么写的**：第 13 步①表格「`erpnext/setup/doctype/company/company.py:418-423` | `create_default_accounts()` 是 … 直接 import 后调用」。
- **实际**（L964 读 415–436）：418 是空行，**419** 才是 `def create_default_accounts(self):`，420 是 `from ... import create_charts`，423 是 `create_charts(self.name, ...)`。探针在 L955 与待验表里写的是 `company.py:420`。
- **属于哪类问题**：技术细节轻微失准（起始行多包了一个空行）。**结论不受影响**，`:423` 独立由 L982 的 grep 结果证实。

#### A-6【强度 · 内部张力，但忠实于当时的会话】「当前唯一能让自制科目表建出来的走法」

- **记录怎么写的**：第 13 步②说 zelin 三钩子「是当前唯一能让自制科目表建出来的走法」；同一步⑥又说 `custom_chart` 是「理论上还有第三种形态」（未验，登记 LG-102）。「唯一」与「还有第三种」在同一步内并存。
- **原始对话实际是什么**：L1012 Claude 对用户的原话就是「那三个钩子不是风格选择，**是当前唯一能让自制科目表建出来的走法**」，而 ⑥ 的第三路也是同一条消息里给的。
- **属于哪类问题**：**不是转写失真**——记录忠实复制了 Claude 当场的表述，且把 ② 明标为「推论」。列此是因为该强度与 A-1（A 案未实测）、⑥（第三路存在）合起来看偏高，供你判断是否要在记录侧加限定语。

#### A-7【尾部表未随讨论推进】决策汇总表与被否决方案汇总表仍是占位

- 决策汇总表：`（待第 1 步用户认可后填入）| — | — | — | —`，而九条决策点已在第 14 步全部定案。
- 被否决方案汇总表：`（暂无）`，而决策 5 的 B 案已判「机制上不可行」、另有多个选项在第 5～14 步被否。
- **属于哪类问题**：与契约 §3.3 / §3.4「累积所有已确认决策 / 登记所有被否决方案」不符。属收尾待办、非转写失真；列此是因为你要在收尾时定这两张表。

#### A-8【低 · 供你判断】发现十一指出项目概况「过期条目」语气不足，但缺口清单里没有对应条目

缺口清单表头是「哪份产物」（不限图谱），现有 GAP-001／GAP-002 两条都指需求图谱。发现十一明写「`docs/项目概况.md` 已记『country_wise_tax.json（China 条目仍是 17%、早已过期）』，但那句话的语气是『有一处过期数据』……实际后果比『过期』重」——若缺口清单按 Spec 只收图谱，则无问题；若收全部产物，则缺一条。**我未读项目概况（按分派约束），仅据记录自身提出。**

### B. 核过、无问题的项

#### B-1 V-18 回报的数值与技术细节，逐项对齐

| 项 | 回报（L955） | 记录 | 判 |
|---|---|---|---|
| 判定 | `no-go`（第一段）／`go`（第二段 13% 模板） | 同 | ✅ |
| Account 条数 | 267 条 | 267 条 | ✅ |
| JSON 节点数 | 递归 266 节点，`in_json_not_in_db` 为空 | 同 | ✅ |
| 增值税明细 | `2221001`–`2221020` 20 条全建出 | 同 | ✅ |
| 明细位置更正 | `2221001`–`2221010` 挂 `2221000`（10 条）；`2221011`–`2221020` 是 `2221 应交税费` 直接子级（10 条） | 同（发现十三） | ✅ |
| 六个顶层 `root_type` | 全对 | 同 | ✅ |
| NSM／孤儿／重名 | 无坏行、孤儿父引用 0、重名后缀 0 | 同 | ✅ |
| 覆盖点数 | 全仓仅 5 处查 `override_whitelisted_method` | 同（并逐个列出 5 个行号） | ✅ |
| 覆盖/原生分叉 | 覆盖得 6 个顶层键；原生 `None`；`get_charts_for_country("China")` 原生只有 `['Standard','Standard with Numbers']` | 同 | ✅ |
| `verified/` 计数 | 73 个 json 里 `cn*`／`China*` 为 0 | 同 | ✅ |
| 报错链 | `document.py:513` → `:1454` → `:1260` → `company.py:348` → `financial_report_template.py:144` | 同，且细化为 `:143 coa=get_chart` / `:144 if coa.get(...)`（L972、L991 原文证实，比回报更准） | ✅ |
| `Error Log`（第一段） | 不增，纯抛栈不写日志 | 同 | ✅ |
| `Error Log`（第二段） | 跑前跑后均 1 条，那条是 09-21 的无关报错，两种 LIKE 查法均空 | 同 | ✅ |
| HTTP 边界 | `POST /api/resource/Company` 得 500，异常类型与两处行号逐字相同；白名单路由正常返回 app 那份表 | 同 | ✅ |
| 退路验掉 | 崩在 `:349 create_default_accounts()` 之前；单独实测 `create_charts(..., '小企业会计准则(2024)', None)` 不抛错、静默返回、建 0 个 Account | 同 | ✅ |
| 税模板 | `P13专票含税 - V18B`／`P13专票未税 - V18B` 都建出；`account_head` 指向 `2221005`（`root_type=Liability`、`account_type=Tax`、父 `2221000`） | 同（去掉了测试站 ` - V18B` 后缀，属测试实例标记，不是载荷细节） | ✅ |
| 野科目 | `222105`／`22210005` 跑前跑后均空，新增 Account 0 条（267→267） | 同 | ✅ |
| 按名命中旁证 | `description` 为 `销项税额 @ 13`，由 `account_name` 拼出 | 同 | ✅ |
| 原科目号 md5 | 与 zelin 原文件一致 | 同（记「探针 md5 核过与 zelin 原文件一致」） | ✅ |
| 默认科目挑错 | `default_receivable_account`=`2203 预收账款`；`default_payable_account`=`2211010 职工工资`；成因 `company.py:425-434` 无 `order_by` | 同（发现十二） | ✅ |
| VAT 科目 | 无编号、`account_type=Tax`、父 `2221000`、来自 `on_update:357` → `create_default_tax_template()` 读 `country_wise_tax.json` 的 China 条目、被 17% 模板引用、`华东弹簧` 上同形存在 | 同（发现十一） | ✅ |
| zelin `get_chart` 缺陷 | 全不匹配时 `return chart`，`chart` 是最后读到的原始文件文本（str），实测返回 Taiwan 那份 json 的字符串；原生返回 `None` | 同（发现十四） | ✅ |
| 强喂手法的限定语 | 「探针脚手架 monkeypatch 强喂，**非 B 案可用机制**」 | 同，限定语保留 | ✅ |

**无一处数值被改动。** 唯一未落记的探针结论是 A-1、A-2 两处「没验到 / 顺带做了」，见上。

#### B-2 探针「没验到的」四条的保留情况

| 回报原文 | 记录 | 判 |
|---|---|---|
| HRMS 同钩子交互完全没触及（本 bench 未装 HRMS，无数据） | 第 13 步⑤原样保留并加引号；LG-101 再记一次「**V-18 明确报告此项完全未触及**（本 bench 未装 HRMS）」 | ✅ |
| A 案（zelin 原样三钩子）未经实测 | **无** | ❌ 见 A-1 |
| 没去找「能让 app 科目表进入原生路径的别的正规机制」 | 第 13 步⑥原样引用；LG-102 记「V-18 明言未找过」 | ✅ |
| 另有七项（修掉 `:348` 后的完整原生路径、另外 3 份中式科目表、体量与并发、`existing_company` 与 COA Importer 分支、升级边界、前端呈现、zelin `set_company_default` 其余 4 个函数） | 未逐条列（在 `C待验表` 复核建议节） | ⚠ 属概括压缩，不报为违规；仅提示第 15 步架构图写了「科目表 JSON ×4」而只有 1 份经测 |

#### B-3【重点核项 2】「查明 zelin 为何必须挂 Company 三钩子」的归属 —— 已核，归属正确

- L955 回报全文 grep `钩子`／`ignore_chart_of_accounts`／`before_insert`／`doc_events`：**只有 1 处提到三钩子，即「A 案（zelin 原样三钩子）未经实测」**。回报**从未**给出这个解释。
- L969／L979／L984 的 thinking 显示是 Claude 自己接上的（「zelin's `before_insert` setting `ignore_chart_of_accounts=True` skips that entire block」），并在 L971 亲自读了 `company.py:345-352` 验证守卫结构。
- 记录把它标为「**本步最有价值的推论**」、放在第 13 步②、议题 `提出者` 为「Claude（派出的探针回报）」。**归属正确，且明标为推论而非实测。** 已核，无问题。

#### B-4【重点核项 3】LG-102 的两处行号 —— 已核，Claude 确实读了

- L981／L982：`grep -rn "create_charts(" erpnext/` → 结果含 `chart_of_accounts_importer.py:106: create_charts(company, custom_chart=forest, from_coa_importer=True)` 与 `company.py:423`。✅
- L972：读到 `chart_of_accounts.py:13-16`，含完整签名 `create_charts(company, chart_template=None, existing_company=None, custom_chart=None, from_coa_importer=None)` 与 `:16 chart = custom_chart or get_chart(...)`。✅
- L986／L991：读 `chart_of_accounts.py` 16–24 与 31–80，结果含 `"account_name": child.get("account_name") if from_coa_importer else account_name` —— 支撑记录里「`from_coa_importer=True` 会改 `_import_accounts` 的行为（`account_name` 取值路径不同，Claude 已核）」这句。✅
- 记录对探针「没去找别的正规机制」的引用与回报原文一致。✅ 已核，无问题。

#### B-5【重点核项 4】第 12 步那处「说过头了」的更正 —— 依据全部有对应读取动作

| 记录引的依据 | 原始对话里的读取动作 | 判 |
|---|---|---|
| `boot.py:446`（`frappe.get_all("Workspace Sidebar")`） | L914 → L918 结果逐行显示 442–477，446/447 即 `get_all("Workspace Sidebar", fields=[...])`，449/450 即 `auto_generate_sidebar_from_module()` + `extend` | ✅ |
| `boot.py:507`（`sidebar_items[sidebar_title.lower()]`） | **本段内**：L1043 → L1044（`sed 442,510 \| grep lower` 得第 66 行 ⇒ 507）；L1048 → L1049（`sed 505,512` 直接显示该行） | ✅ |
| `sidebar.js:286`（`workspace_title = sidebar_title.toLowerCase()`） | **本段内**：L1048 → L1049 结果 `286: this.workspace_title = this.sidebar_title.toLowerCase();` | ✅ |
| `sidebar.js:31` | L912 结果 `sidebar.js:31: this.sidebar_data = frappe.boot.workspace_sidebar_item[this.workspace_title];` | ✅ |
| `sync.py:120` 的 `app_level_folders` | L385 → L390 结果 `120: app_level_folders = ["desktop_icon", "workspace_sidebar", "sidebar_item_group"]` | ✅ |

第 12 步原文确实写了那句（记录第 721 行 D 选项「缺点」列），故「承认说过头了」这件事**成立**；唯一瑕疵是引文插了「同名」二字（A-4）。

#### B-6【重点核项 6】`用户回答` 格 —— 三处裁决全部有据，且没有扩大认可范围

| 记录位置 | 记录写的 | 用户实际说的 | 判 |
|---|---|---|---|
| 第 13 步 | 「**确认改判** —— 决策 5 由 B 改判 A。」 | L1020 `三条均按你的建议`（L1012 结尾正好问了三条：决策 5 改判 A？决策 9 取 D？决策 7 取 B？） | ✅ |
| 第 14 步① | 「决策 5 改判 A、决策 9 取 D、决策 7 取 B，三条均按建议。」 | 同上一条 | ✅ 忠实展开，未超出 Claude 所问的三条 |
| 第 14 步⑤ | 「选 **A（模块最少）**。」 | L1069 `A`（Claude 问「请裁决：议题三取 A 吗？」） | ✅ |
| 第 15 步 | 「待用户回答」 | L1116 `均同意。`（本轮在跑，尚未落步） | ✅ **未把 `均同意。` 写进任何格，也没有借它扩大认可范围** |

**特别就你关心的那点**：`均同意。` 是对议题四与议题五两个议题的一并认可，记录**完全没有动它**——既没写成对更多东西的认可，也没提前回填。收尾时把它落进第 16 步（或回填第 15 步）由你定，但目前的记录状态在这一点上是干净的。

#### B-7【重点核项 7】行号抽查 —— 7 条全部命中（要求 ≥5）

| 引用 | 读取动作 | 结果 | 判 |
|---|---|---|---|
| `frappe/__init__.py:899-901` | L1031 → L1032（`grep "def get_module_list" -A 4`） | 899 def / 901 `return get_file_items(get_app_path(app_name, "modules.txt"))` | ✅ |
| `installer.py:749-755` | L1025 → L1026（`grep "def add_module_defs" -A 12`） | 749–755 逐行，含 `frappe.new_doc("Module Def").insert()` | ✅ |
| `workspace_sidebar.py:239-252` | L922 → L923（`grep "def auto_generate_sidebar_from_module" -A 28`） | 239 def、250 `header_icon = "hammer"`、252 return；255 `get_module_info` 的 entities 正是 `["Workspace","Dashboard","DocType","Report","Page"]` | ✅ |
| `sidebar.js:709-744`（`resolve_sidebar` 四级优先） | L1031 → L1032（700–725）＋ L1038 → L1039（725–770） | 709 `resolve_sidebar(entity, module) {`、710 `candidates = this.get_workspace_sidebars(entity)`、第 1 条规则 `if (this.sidebar_title && candidates.includes(this.sidebar_title))`、函数在 744 前收尾 | ✅ |
| `boot.py:507` | 见 B-5 | — | ✅ |
| `templates/base.html:53` | L1088 → L1093 | `base.html:53: window.socketio_port = {{ (frappe.socketio_port or 9000) }};` 逐字一致 | ✅ |
| `frappe-crm/frontend/src/socket.js` | L1074 → L1079（grep）＋ L1083 → L1084（全文 cat） | `window.socketio_port \|\| 9000`、自己拼 `url = protocol://host:port/siteName`、`io(url, {withCredentials:true})` —— 记录描述逐项对得上 | ✅ |
| 附加：`boot.py:166`（`load_conf_settings`） | L1088 → L1093（`sed 160,170`） | 166 即 `for key in ("developer_mode", "socketio_port", "file_watcher_port")` | ✅ |

架构图里的 `socketio 9100`、`MariaDB 11.8`、`use_responses=False` 不在本段读取，但在本 Session 更早的工具结果（L24／L41／L125）中有据，非凭空。

#### B-8 第 14、15 步其余内容 —— 已核，无问题

- 九条决策点定案汇总表：与第 5～14 步各步结论逐条对得上；决策 5 行标注「V-18 `no-go` ⇒ 由 B 改判 A」，与实际一致。✅
- 发现十五三处机制串联（`modules.txt` → `Module Def` → 自动 hammer 侧栏）：三条行号全部实读（见 B-7），「凡没有同名 `Workspace Sidebar` 记录的模块当场在内存里生成一份」与 L923 原文 243–251 逐行一致。✅
- 议题三 A/B/C 三选项与「推荐 A 三条理由」：与 L1065 给用户的消息一致，缺点列没有被削弱（B 的「6 个自动侧栏与 CR-010 直接相悖」、C 的「报表列表分组里客户看到财税与产能报表同组」都在）。✅
- 「⚠ 一处小未验：`modules.txt` 能否为空本轮未验」：与 L1051 thinking 的自我限定一致，未验标注保留。✅
- 边界检查四题 + 「公开接口 16 项命中 Spec『超过 10 个』拆分信号」：与 L1065 一致，拆分信号如实记入而非抹掉。✅
- 第 15 步「共享推送侧的确切含义」：结论「重写的不只是监听，还有连接建立」有 L1084／L1093 两处实读支撑，且记录明说这比图谱 §7.3 的限定语「更具体」（不是推翻图谱），强度得当。✅
- P-4 的自我否定（「按 D 步 Spec 不允许悬空预留，它不该进前瞻性设计清单」）与过度设计风险交代（「P-1 锚的 EX-002 是实验性需求、本身可能不做」「真正的支撑理由『远期 React 全面重写』在图谱里没有需求编号」）：与 L1081／L1095 thinking 一致，**没有把锚不上的写成锚上了**。✅
- 遗留问题表三条：LG-100 溯到第 11 步（记录 681 行）✅；LG-101 与 LG-102 溯到第 13 步，且归属标注正确（LG-102 写「本轮 Claude 查出，V-18 明言未找过」）✅。
- 缺口清单两条：GAP-001 对照图谱第 314 行 LG-099 原文成立（图谱确实写「阶段 B 的 11 项与阶段 C 的 10 项……不在本图谱内」并把 CRP 负荷报表、SPC·CPK 当作例子，而 IM-010／IM-011 就在图谱 §3.2 里）✅；GAP-002 对照图谱第 50 行 CR-002 原文成立（确有「+ `Translation` 记录做运行时最高优先覆盖」这半句）✅。

---

## 三、重点核项 5 的结论（单列）：四个锚点是否真实存在

核对 `docs/01-需求摸底/0-P1文档/需求图谱.md`（50,922 字节）：

| 预留 | 记录给的锚点 | 图谱中是否存在 | 位置 | 内容对得上吗 |
|---|---|---|---|---|
| **P-1** 事件协议与业务代码解耦 | **EX-002** | ✅ **存在** | 第 133 行（实验性需求表）「演示录屏与自动记录／移动端查看」；第 260 行依赖分析；第 56 行 CR-008 关系列写「↔ EX-002（共享事件协议与推送侧）」 | ✅ 对得上。记录引的评估触发条件「演示成功一次之后，若要复用演示过程、或需远程展示」与图谱第 133 行**逐字一致**；记录称「图谱 §7.3 亦明写 CR-008 ↔ 远期 React」，图谱第 214 行确为「CR-008 ↔ 远期 React 重写 UI。⚠ 只共享推送侧」✅ |
| **P-2** 译名做成可反复修正的配置文件 | **IM-003** | ✅ **存在** | 第 85 行（§3.2 表）「内置摸底问点清单」；第 257 行依赖分析 | ✅ 对得上。记录引「图谱定位节明写『该清单不是一次性交付物……这反过来要求译名做成可配置文件』」，图谱第 34 行原文为「该清单不是一次性交付物，而是长期跟厂边调研边改进的机制起点（第 24 步用户裁决）——这反过来要求译名做成可配置文件、而非一次性翻译动作」✅ 省略号处所省内容不改变原意 |
| **P-3** `modules.txt` 留出第二个模块的位置 | **IM-010／IM-011** | ✅ **两条都存在** | 第 103 行 IM-010「CRP 产能负荷报表」、第 104 行 IM-011「SPC 控制图 + CPK」；第 241、245 行依赖图 | ✅ 对得上。图谱写 IM-010 的缺口正是「22 个报表 + 8 个 dashboard chart 无一是负荷口径」、IM-011 是 SPC 控制图+CPK，记录称「都是 Report，需要模块」属设计判断、与图谱内容不冲突 |
| **P-4** desk js 覆盖登记表 | **锚不上任何编号**（记录自述） | — | — | ✅ 记录自己就说锚不上，并说明正确归属是决策 7 的组成部分。**如实，无悬空伪装** |

**结论：四个锚点里该存在的三条全部真实存在、内容对得上；第四条记录自己声明锚不上并交代了处置。无悬空预留被伪装成有锚。**

一处需你知道的背景（不是本段的失真）：图谱 LG-099（第 314 行）声称「CRP 负荷报表、SPC·CPK……不在本图谱内」，与 IM-010／IM-011 确实在图谱 §3.2 内相矛盾——这正是记录已登记的**发现一 / GAP-001**。故 P-3 的锚点在编号层面成立，图谱内部那处矛盾已另有条目管着。

---

## 四、汇总

**本段查出 8 处，严重度排序：**

| # | 问题 | 类别 | 严重度 |
|---|---|---|---|
| A-1 | 探针明说「A 案未经实测」，第 13 步全文无痕（而这一步正是改判 A） | 漏记 · 否定表述丢失 | **高** |
| A-2 | 探针顺带删掉 4 条历史孤儿数据（对站点的实际改动）+ Claude 报给用户的站点核验结论，均未落记 | 漏记 | **中高** |
| A-3 | 发现十四把崩点归到「发现十一」（发现十一无崩点） | 交叉引用指错 | 中 |
| A-7 | 决策汇总表与被否决方案汇总表仍是占位，而九条决策已全部定案、B 案已否 | 结构未随讨论推进（§3.3／§3.4） | 中 |
| A-4 | 第 14 步引第 12 步原文时在「」内插入「同名」二字 | 引文非逐字（语义未变） | 低 |
| A-6 | 「唯一能让自制科目表建出来的走法」与同步⑥的第三路、与 A-1 并存 | 强度偏高（**忠实于会话原话**，非转写失真） | 低 |
| A-5 | `company.py:418-423` 起始行多包一空行（def 实为 419，探针写 :420） | 技术细节轻微失准 | 低 |
| A-8 | 发现十一指出项目概况「过期条目」语气不足，缺口清单无对应条目（取决于 Spec 是否把缺口清单限定在图谱） | 待你判断 | 低 |

**最要紧的一处**：A-1。第 13 步是决策 5 由 B 改判 A 的那一步，记录在此把 A 写成「zelin 的形态、当前唯一可行」，却丢掉了探针那句明确的否定表述——「A 案未经实测，`erpnext_china_create_charts` 与三个 doc_events 刻意没抄进测试 app」。下游 D 步读本记录会得到「A 已验」的印象，而实测覆盖面其实是「B 已证不通 + A 未测」。该否定表述在 `C待验表.md` 第 232 行仍在，补记只需一句。

**未能核对的**：无。transcript 可得（L950–L1162 完整可读），本段全部比对均对照原始对话完成。
