# P1-S2-R7 · devBlueprint F 步（`build`）回执

**产出步骤**：P1-S2 演示蓝图 · Round 7 · devBlueprint **F 步**（`build`）
**唤起方**：用户「继续开发项目，之前应该已经定了开发顺序了，如果没有新的裁决点，直接落路线文档，不再单独落讨论记录」→ 流程中枢；F 步由 R6 D 步出口（状态值 `架构已产出`）经 E 步路由而来
**依据**：[需求图谱](../../0-P1文档/需求图谱.md) v1.1 ＋ [架构文档](../../0-P1文档/架构文档.md) v1.0 ＋ ADR-0001…0012 ＋ [P1-S2-R2-A侵入度与工作量](../R02-图谱讨论续/P1-S2-R2-A侵入度与工作量.md)（43 项）＋ **用户 2026-09-26 两处裁决**
**规划层级**：Phase 级（分解单位＝Stage）
**打tag**：`P1-S2-R7-start`（F 步 `打tag=前`，**本地未推**）
**日期**：2026-09-26

**产物**（落点按 `流程规范.md` §11.1）：

| 产物 | 落点 | 状态 |
|---|---|---|
| [路线文档](../../0-P1文档/路线文档.md) v1.0（421 行） | Stage 外的 `0-P1文档/`，文件名不带 R 号 | 已产出 |

---

## ⚠ 本步的编排偏离：E 步未单独落讨论记录

**用户指令**：「如果没有新的裁决点，直接落路线文档，不再单独落讨论记录」。

**读盘核实结果：条件不成立——有两个裁决点。** 用户前提里"已定了开发顺序"**部分成立**：

| 已定的 | 未定的 |
|---|---|
| **需求级次序**：图谱 §7.1 四条硬次序（术语链／服务端修正链／科目表链／漏斗链，全汇向 CR-012）＋ 工作量表 A-1…A-7 分组 ＋ 阶段 A/B/C 分档 | **议题划分本身**：全库搜 `P1-S3`／`S03-` **零命中**；C 步架构讨论里无分解内容；架构文档各模块的「所属分解单位」只写到"P1 后续 Stage"这一档，未落到具体 Stage 号 |

且 E 步 Spec 明令「**议题划分必须走对比，不允许直接给一个划分**」。

**处置**：两个裁决点以 `AskUserQuestion` 就地取裁，**不开 E 步讨论记录文件**（遵从用户指令的实质——不为流程形式多产一份文件），裁决结果直接作本步依据。

| 裁决点 | 选项 | 用户裁决 |
|---|---|---|
| **Stage 划分** | 甲（直接映射 A-1…A-7，7 个 Stage）／乙（按硬次序层级，5 个）／**丙（财税整块＋前置最小，6 个）** | **丙** |
| **P1 边界** | **阶段 B 留在 P1、改收口标志** ／ 阶段 B 转 P2、保留收口标志 | **前者** |

**代价如实交代**：E 步 Spec 的其余四节预设提示词（功能盘点／后续议题划分/依赖梳理／接口契约预定义／返工风险识别）**未经独立讨论轮**，由本步在产物内一次落成。**故那四节内容是本步的判断，未经用户逐节确认**——复核建议 1 指名了其中最该看的两处。

---

## 逐项执行结果

| 任务 | 对应依据 | 落地位置 | 结果 | 完成时间 | 说明 |
|---|---|---|---|---|---|
| TS-001 | 产物规格 · 文档头部（版本／日期／前置依赖／规划层级／版本变更记录） | [路线文档.md](../../0-P1文档/路线文档.md) §头部＋版本变更记录 | ✅ 完成 | 2026-09-26 | 含 E 步未落讨论记录的说明 |
| TS-002 | 本次分解的边界（P1 新收口标志／分解出什么／不在本次内的） | 同上 §一 | ✅ 完成 | 2026-09-26 | 含三处量级缺口的如实交代 |
| TS-003 | **与裁决的差异**（本步对方案丙的两处修正） | 同上 §二 | ✅ 完成 | 2026-09-26 | 两处都是依赖方向错，见下「偏离与暂停」 |
| TS-004 | 产物规格 · 划分总览（Phase 级原子议题加 `Workflow` 列，复合加子议题数） | 同上 §三 | ✅ 完成 | 2026-09-26 | 6 行＋阶段小计 |
| TS-005 | **自评审① 补出的「明确记为不做的三条」** | 同上 §三末 | ✅ 完成 | 2026-09-26 | DF-001／EN-002／EX-002，**初稿漏项，机械对账查出** |
| TS-006 | 产物规格 · 各议题详情（目标／类型／纳入功能／完成标志／前置依赖／产出后效／执行清单与边界；原子另写 Workflow 及偏离） | 同上 §四 | ✅ 完成 | 2026-09-26 | 6 小节；S8G 另列 4 个子 Stage 表与子 Stage 依赖 |
| TS-007 | 产物规格 · 议题依赖图（ASCII，标注依赖与复合议题子关系） | 同上 §五 | ✅ 完成 | 2026-09-26 | 另加「四条硬次序在本划分里的落点」逐条对账表 |
| TS-008 | 产物规格 · 潜在返工风险（`RW-` ＋两位序号） | 同上 §六 | ✅ 完成 | 2026-09-26 | **RW-01…RW-09 九条**，连续无跳号；另附缺口自述 |
| TS-009 | 产物规格 · 跨议题接口契约预定义 | 同上 §七 | ✅ 完成 | 2026-09-26 | 4 条，逐条判寿命 |
| TS-010 | 产物规格 · 被否决的划分方案 | 同上 §八 | ✅ 完成 | 2026-09-26 | 甲／乙两条，含对比维度与否决理由 |
| TS-011 | 产物规格 · 遗留问题（`RL-` ＋两位序号） | 同上 §九 | ✅ 完成 | 2026-09-26 | **RL-01…RL-07 七条**，其中 **RL-06／RL-07 是本步新立**（RL-07 在 Stage 收口扫查阶段才查出，见下「本步查出的两处上游缺口」） |
| TS-012 | 预设提示词 · 非本级功能的去向（Phase 级 → 开发范围节，不经延迟需求） | 同上 §十 ＋ [P1-概况.md](../../P1-概况.md) 开发范围节 | ✅ 完成 | 2026-09-26 | 阶段 C 10 项逐项列名＋人日＋理由 |
| TS-013 | 长效信息移交 · 跨议题接口契约（逐条判寿命，有本级外消费方的升上一级） | [P1-概况.md](../../P1-概况.md) 契约一状态改「F 步定稿」＋ [开发守则.md](../../../开发守则.md) §跨 Phase 接口契约表加指针行 | ✅ 完成 | 2026-09-26 | **判定：本体留 P1 概况、守则只存指针**，理由见下「新增约定」1 |
| TS-014 | 回填上级概况（各议题＋类型＋完成标志＋Workflow／子议题数；级联指针） | [P1-概况.md](../../P1-概况.md) 级联指针＋开发范围表 | ✅ 完成 | 2026-09-26 | 表由 5 列扩为 7 列（加完成标志、工作量） |
| TS-015 | **改 P1 旧收口标志表述**（与新裁决相抵） | [P1-概况.md](../../P1-概况.md):42 新写＋`:56` 旧句改写 | ✅ 完成 | 2026-09-26 | 旧句「本 Phase 收口标志改为：S2 产出三份产物」已标为被取代 |

---

## 自评审执行结果

> F 步 Spec 的「全量验证」节改为本节（本步产物是文档，无测试门）。**八项逐项报执行方法与结果，不是逐项声明「已做」。** 机械可核的五项写了脚本跑（`tmp-roadmap/check.py`、`check2.py`，**均为临时产物、已随本步清理**）。

**① 功能全覆盖且不越界 —— 通过，但查出并修了一处漏项**

方法：正则从需求图谱抽全部需求表行首编号得 **37 条**；再从路线文档各议题「纳入功能」格抽编号做归属对账。

- **初稿结果**：已归属 34 条、去重后 34（**无重复归属**），**未归属 3 条**（DF-001／EN-002／EX-002），其中 **EN-002 在初稿里零提及**。
- **判定**：自评审①要求"每个功能都被分到某个议题**或明确记为不做**"，初稿缺后半句 ⇒ **是真漏项，不是设计选择**。
- **已修**：补 §三末「明确记为不做的三条」表，逐条写分级／不做理由／将来做的触发条件。**其中 EN-002 另牵出一处张力**（图谱给拼音检索那半的唤醒条件名义上是"与译名改造同批"即 S6，而本步判为不做）⇒ 立 **RL-06**，并显式写明"这是本步的判断，不是用户裁决"。
- **越界核验**：路线文档提到的编号**全部**在图谱定义内，`extra = 空`。

**② 三处所属议题对账 —— 通过**

方法：分别从①划分总览表行、②各议题详情「纳入功能」格、③议题依赖图抽议题号与编号，两两取差集。

- 议题号三处一致：`S3 S4 S5 S6 S7 S8G`（依赖图另含四个 `S8G-S*` 子 Stage，是下一层、不参与本项对账）。
- 编号：S3(3)／S4(4)／S5(5)／S6(4)／S7(7) **五个 Stage 逐一"一致"**，差集全空。
- 依赖图里的议题**全部**在划分总览中。

**③ 引用真实性 —— 通过**

方法：抽路线文档全部 `ADR-\d{4}` 引用，逐个在 `0-P1文档/` 下查同前缀文件是否存在。

- 引用 7 份（ADR-0001／0004／0006／0007／0008／0010／0011），**7 份全部解析**。
- 需求编号的真实性已由自评审①的双向差集覆盖（`extra = 空`）。
- 议题依赖图里的议题都在划分总览中——同②。

**④ 首个议题不溢出 —— 通过**

首个议题 **S3 演示前置只有 3 条需求、3–4 人日**，是六个议题里最小的。逐条自问"有无该按 YAGNI 延后却塞进来的"：

- **CR-001 术语标准**：硬前置（图谱 §7.1 链①起点），不做则 CR-002 定不了名。留。
- **IM-001 参数复核**：不做则样本带错数值（图谱明写"这不是'最好做'而是不做会出错"）。留。
- **IM-003 摸底问点**：**这条最该被质疑**——它不被任何需求依赖（图谱 §7.6 列为独立项），按"对下游有多少用"判可以延后。**但 Spec 复核建议第 1 条明令"判它之前先问这个功能有没有独立于本次分解的目的"**：IM-003 是**定位的直接产物**（演示编排内置摸底问点），且**它的产出要在演示现场用**——演示只有一次，问点没准备好就白失一次摸底机会。**故留**，理由写进复核建议 1。

**⑤ 完成标志可验证 —— 通过**

方法：① grep 模糊表述黑名单（`做完了`／`基本可用`／`大致完成`／`基本完成`）—— **零命中**；② 逐个议题读完成标志，判"第三方能否判定真假"。

- 全部 6 个议题＋4 个子 Stage 的完成标志都带**可观测判据**：具体数值（`planned_qty` 实测 5264）／具体端口（8000 与 9100 均可达）／具体计数（23 个节点各响应一次、20–40 词经抽检）／具体核对动作（抽一个物料手算复核、拿一组已知数据手算复核）。
- **最虚的一个已指名**，见复核建议 2。

**⑥ 依赖无环 —— 通过，但查出并修了两处表述错**

方法：脚本按 `S3→S4→S5→S6→S7→S8G` 给序号，逐议题抽「前置依赖」格里的 Stage 号，凡前置序号 ≥ 自身即报违反。

- **初跑报 3 处违反**：(S4,S7)／(S5,S6)／(S5,S7)。**逐处查证：全部是「前置依赖」格里混写了下游约束或并行说明**，不是真环——S4 格写了"但必须在 S7 建数据之前完成"，S5 格写了"但必须在 S6 之前"与"可与 S3、S4 并行"。
- **判定：仍是产物缺陷**——按产物规格，下游约束属「产出后效」列、并行说明属依赖图，混进「前置依赖」会让下游读错方向。**已修**：把两格的下游约束移入「产出后效」（并加 ⚠ 前缀保住其强度），并行说明改为指向 §五依赖图。
- **重跑结果**：`S3 无／S4 无／S5 无／S6 ← S3 S5／S7 ← S3 S4 S5 S6／S8G ← S3 S4 S5 S7`，**违反方向 = 空**。
- S8G 内四个子 Stage **彼此无依赖**（IM-013→IM-014 与 IM-009→IM-008 都在同一子 Stage 内部），亦无环。

**⑦ 各议题所需依据齐备 —— 通过**

逐议题自问"进入这个议题时，执行者从哪读到它该做什么"。**每个议题详情都有「本议题执行所需的清单与边界」小节**，条数：S3 六条／S4 十条／S5 十条／S6 十条／S7 十三条／S8G 九条。

内容是把**只存在于讨论记录与调查报告里**的清单落进来，例如：CR-001 的范围限死（只统一两类，不试图定全部术语）／`context` 解决不了撞名／A 案四处缺口的补法／HRMS 必须用 `version-16` 分支／zelin 那个 `222105` bug／`expenses_included_in_valuation` 是 v13/v14 旧名／事件协议的 `seq` 与 `user` 为何必需／Raven 三问必须先实测再进 S7。**讨论记录不是下游的读取入口**这条已落实。

**⑧ 长效信息已移交、上级概况已回填 —— 通过**

- **跨议题接口契约**：4 条落路线文档 §七，**逐条判寿命**。契约一（事件协议）在 P1 概况的状态由「D 步识别」改为「**✅ F 步定稿**」，并加寿命判定段。
- **非本级功能**：阶段 C 10 项落 P1 概况开发范围节（Phase 级规划的规定去向，**不经延迟需求**）。
- **上级概况回填**：P1 概况级联指针改指 S3（待起）、S2 标已收口；开发范围表由 5 列扩为 7 列（加完成标志、工作量），6 个议题逐行填，原子议题写 Workflow、复合议题写子议题数。
- **另修一处相抵**：P1 概况 `:56` 的旧收口标志句已改写（见 TS-015）。

---

## 本步查出的两处上游缺口

> 两处都不是本步产物的问题，是**依据本身的缺口**。按 play-build 纪律 1「依据是唯一权威源」，本步不擅自改上游产物，只登记并指名。

**缺口一：需求图谱 37 条里有 3 条没被分入任何议题**（自评审①机械对账查出）

DF-001／EN-002／EX-002。**这一处已在本步产物内闭合**——补 §三末「明确记为不做的三条」表，逐条写分级、不做理由、将来做的触发条件。**判据是自评审①的后半句**（"或明确记为不做"），初稿只做了前半句。**EN-002 另牵出一处张力** ⇒ 立 RL-06。

**缺口二：⭐ `PH-P1002` 五例中有两例在需求图谱里没有落位**（Stage 收口扫查阶段查出 ⇒ RL-07）

| 例 | V-07 分组 | 在图谱里的落位 |
|---|---|---|
| ① 仓库列表页空白 ＋ ⑤ 树形 DocType 从侧栏点进空白 | `{1,5}` | ✅ **EN-001**（且已降为回归检查项，DEC-037） |
| ② 采购工作区侧栏「物料与价格」卡片不显示 | `{2}` | ✅ **LG-073 改判为死代码**（`add_card()` 全仓零调用者、**刷新也永不出现**，不属"不刷新即不显示"） |
| **③ BOM 保存后「提交」按钮不出现** | `{3}` | **❌ 无落位** |
| **④ 用户报"很多操作做完后不能第一时间显示"** | `{4}` | **❌ 无落位** |

**为何是真缺口而非本步漏划**：`PH-P1002` 在 P1 概况登记册里的唤醒条件是「**客户演示前**」（用户 S1-R6 裁决），故按登记册这两例应在 P1 内做；而 **V-07 实测判定四例分属三条路径、"不能修一处同修"**，故 EN-001 覆盖不了它们。**本步不补**——F 步 Spec 明令「议题划分里的需求编号必须来自需求图谱，**不新造需求**」。

**须在 S6 开工前定**，三条处置路径与一条低成本验证建议见路线文档 RL-07。**其中那条建议最值得先做**：LG-071 指出 S1 全程实操都在 realtime 死的环境下做的，这几例"可能因 realtime 接通而消失"只是假说、须重跑那组操作验证——而 **realtime 已在本项目修好（9100 两头同挪并经用户实证），故验证成本很低**。

---

## 偏离与暂停

| # | 偏离项 | 原定 | 实际 | 理由 |
|---|---|---|---|---|
| 1 | **E 步未单独落讨论记录** | devBlueprint 步骤表：E 步产 `路线讨论` | 两个裁决点就地取裁，不开讨论记录文件 | **用户明确指令**。代价（E 步其余四节未经独立讨论轮）已在上方专节如实交代 |
| 2 | **修改了用户所选方案丙的次序与两条需求归属** | 方案丙原样：S5 译名与导航 → S6 演示链路；CR-009 落演示链路 | S5 演示链路 → S6 译名与导航；CR-009 移入 S7 | **两处都是依赖方向错，不修即违反自评审⑥**：① 图谱 §7.4——四个 App 每接入一个，译名与导航工作量各增一份，译名在前则**两件事都做两遍**；② 图谱 §7.1 汇流节写的是 `CR-008 → CR-009`，心跳建在事件协议推送侧上，放 S5 会让**前序 Stage 依赖后续 Stage**。**Stage 集合与数目未动** |
| 3 | **各 Stage 的 `plannedDev` D 步执行者由 `CodeX` 改为 Claude** | plannedDev 步骤表：D 步预设执行者 CodeX | 四个走 plannedDev 的议题各写此偏离 | **本项目此前无 CodeX 使用记录**（三份常驻文件与两级概况 grep 零命中）。**已写明"若届时引入 CodeX，按步骤表原值执行即可，本条偏离随之失效"** |
| 4 | **为三条需求类推了人日估算** | 工作量表是唯一估算来源 | IM-004（1–2）／CR-015（0.5–1）／EN-001（0.5） | **工作量表 43 项里这三条零命中**（grep 实核）。已在 §一末与各处标注"**本文件类推**"，并立 **RW-09** |

**暂停项**：无。

---

## 新增约定

| # | 约定 | 类别 | 在哪个任务确立 |
|---|---|---|---|
| 1 | **事件协议契约的本体留 P1 概况，`开发守则.md` 只存一行指针** —— 判据：它唯一的 P1 外消费方（远期 React 重写）**在需求图谱里没有需求编号**，尚不构成已立项的跨 Phase 消费方。**P1 收口时重判一次**：已立为需求则整条升进守则，仍未立项则随 P1 归档 | 位置 | TS-013 |
| 2 | **`开发守则.md` 的跨 Phase 接口契约表用四列**：接口／定义在／消费方／状态。「定义在」列可指向下级容器（本表允许只存指针，不强制全文上移） | 位置 | TS-013 |
| 3 | **路线文档的风险编号用 `RW-` ＋两位、遗留用 `RL-` ＋两位**，一经分配不复用；某条消解后**保留空位**，不把后续编号往前挪 | 命名 | TS-008／TS-011 |
| 4 | **原子议题的「前置依赖」列只装真前置**；下游约束进「产出后效」（可加 ⚠ 保强度），并行说明进依赖图。**混写会让下游读错依赖方向**——本步自评审⑥正是被这三处混写绊了一次 | 位置 | 自评审⑥ |

---

## 未做项

| # | 未做 | 原因 | 归属 |
|---|---|---|---|
| 1 | **E 步的四节预设提示词未经独立讨论轮** | 用户指令不单独落讨论记录；两个真裁决点已就地取裁 | 见复核建议 1——若认为某节须补，回 E 步 |
| 2 | **S3 未起**（文件夹与 Stage 概况未建） | F 步的职责止于产出路线文档与回填概况；**起 Stage 是流程中枢第 5 步的事**，属下一个动作 | 下次「继续开发」时按中枢第 5 步执行 |
| 3 | **Stage 收口的常驻文件契约 §4 全量查未做** | 本回执写完后执行（中枢第 8 步第 5 项） | 本 Session 内紧接着做 |
| 4 | **未做任何 git 提交/推送** | CLAUDE.md「版本管理操作规则」：默认不落库。仅 `P1-S2-R7-start` tag 属流程步骤 `打tag=前` 的例外，且**本地未推** | 待用户显式许可 |
| 5 | **RL-01…RL-05 五条遗留未处置** | 它们是 D 步复核建议提请裁定的事项与已知未验项，**不属本步产物范围** | 各自触发条件已写在路线文档 §九 |

---

## 状态值

| 判据 | 实际 | 判定 |
|---|---|---|
| 路线文档构建完成 | v1.0，421 行，产物规格七个必填节＋三个条件节全部落地 | ✅ |
| 自评审通过 | 八项逐项执行并报方法与结果；**查出并修了两类问题**（漏 3 条不做登记、3 处前置依赖混写） | ✅ |
| 长效信息已移交 | 契约一状态改 F 步定稿＋寿命判定；阶段 C 10 项落开发范围节；守则加指针行 | ✅ |
| 上级概况已回填 | 级联指针改指 S3；开发范围表 7 列 6 行；旧收口标志句已改写 | ✅ |

**状态值**：**`路线已产出`**

**出口**：按 devBlueprint 出口路由，`路线已产出` → **Stage 收口**。

---

## 复核建议

> 按 F 步 Spec 的四类写。**不是「请全面复核」，是指名哪几处最该看。**

**1. 首个议题范围最该再砍的一条 —— 以及 E 步四节未经讨论的代价**

- **该砍的候选是 IM-003 摸底问点清单**（S3 三条之一）。它**不被任何需求依赖**（图谱 §7.6 列为独立项），按"对下游有多少用"判完全可以延后。**本步判留**，理由是 Spec 复核建议第 1 条明令"判它之前先问这个功能有没有独立于本次分解的目的"——IM-003 是**定位的直接产物**，且**产出要在演示现场用，演示只有一次**。**这个判断是本步做的，请确认。** 若认为该砍，S3 只剩 CR-001＋IM-001（2–3 人日），IM-003 可并入 S7（与 IM-004 演示脚本文本同批做）。
- **E 步四节未经独立讨论轮**（功能盘点／议题划分／依赖梳理／接口契约／返工风险），其中**最该复核的是「议题依赖梳理」**——Spec 自己标它「最重要」，且判漏的代价要几个议题后才显现。本步用的是机械对账（脚本核依赖无环＋四条硬次序落点逐条对账）替代讨论，**机械核得住"有没有环"，核不住"有没有漏一条依赖"**。

**2. 完成标志最虚的那个议题**

**S8G-S3 工时与工序**（12–20 人日，五条需求）。它的完成标志五项里，**第①项「kg↔pcs 换算可按批次或公式取值，同规格不同批次钢丝给出不同系数」判不成真假**——"不同系数"是现象，不是判据；到底按什么算、精度要求多少，图谱与工作量表都没给（IM-012 只说"系数是物料级固定 float"这个缺口，没说目标形态）。**而这条要等到 S8G-S3 开工才暴露。** 建议 S8G-S3 的 A 步（需求讨论）第一件事就是把它定成可验证的。

另**次虚的是 S5 的完成标志③**（"Raven 三问各实测一次并记录答得出与否"）——它**只要求记录结果，不要求结果为真**。这是有意的（RW-04 已写明答不出时的分支），但它意味着 **S5 可以在"AI 答不出"的状态下收口**，此时 S7 的 CR-012 数据怎么编就成了开放问题。

**3. 划分与讨论的差异处**

- **两处对用户所选方案丙的修正**（见「偏离与暂停」2）：App 接入前移、CR-009 移入 S7。**这两处改的是用户刚裁决的方案**，虽有图谱明文依据（§7.4、§7.1），但**请确认与原意相符**。
- **S8G 的四个子 Stage 划分沿用工作量表 B1…B4 分组**，未另作对比方案。理由是那四组的依据（图谱 §7.5 内部依赖＋性价比排序）已足；**但严格说这违反了 E 步"议题划分必须走对比"那条技法**——它只约束到本次分解出的那一级（Stage），子 Stage 属下一层。**这是本步的读法，请确认。**
- **三条需求的人日是本文件类推的**，不是工作量表给的（见「偏离与暂停」4）。

**4. 返工风险表的缺口自述**

识别 RW-01…RW-09 时**只往三个方向想了**：未验证的技术假设（01/02/04/06/07）、划分本身引入的次序脆弱性（03/08）、估算不确定性（05/09）。

**没往这两个方向想**：① **外部依赖变更**（zelin 仓库删档或改许可证、四个 App 的 v16 分支停更、ERPNext v16 小版本升级改了本项目依赖的函数签名——本项目**并入式抄 zelin 源码**且**覆盖 4 个 whitelisted 方法**，这类变更的影响面比表内九条都大）；② **用户侧变更**（实地考察后客户需求大改、演示时点提前压缩排期）。

两类都已在路线文档 §六末显式自述，**但未立为 RW 条目**——理由是它们不是"划分做错了"能预防的。**若认为该立，编号从 RW-10 起。**

**5. 拿不准处**

- **S4 与 S5 谁先做，本步没定。** 两者都零前置、可并行，依赖图画成同层。但**它们有一处隐藏交互**：RW-01 说 HRMS 的 `Company.on_update` 可能往 S4 已定型的科目体系里加 HR 科目。**若 S4 先做完再装 HRMS，风险是"污染已建好的科目表"；若 S5 先装 HRMS 再做 S4，风险是"建账时多两层未知钩子"。** 本步的处置是把风险登记（RW-01）＋要求"先在一次性测试公司上验"，**但没有裁定次序**。这处可能需要一次实测才定得下。
- **S7 内部的硬次序②靠开发方案保证，不靠 Stage 边界**（RW-03）。这是方案丙"服务端修正下移"这个选择的直接代价。**若认为该由边界保证，应把 CR-003/CR-004 单独拆成一个极小 Stage**——但那正是方案甲被否决的理由之一（1.5–2.5 人日凭空多造一次 Stage 收尾）。
- **路线文档的行号引用会随后续轮次改写而失效**。本回执正文里的节号（§三／§四一类）是稳定的，**但该产物跨轮就地改写**（§11.1），查证时请按节号找，不要按行号。
