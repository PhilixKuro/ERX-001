# P1-S2 演示蓝图 — Stage 概况

> Stage 级概况：当前 Workflow 是哪条、进行到哪个步骤/轮次、本 Stage 延迟需求登记册。

## 当前编排

**`devBlueprint` Workflow**（非自由编排）。文件：[../../流程体系/workflows/devBlueprint/0-devBlueprint-workflow.md](../../流程体系/workflows/devBlueprint/0-devBlueprint-workflow.md)

**本次规划层级：Phase 级**（该 Workflow「规划层级」节要求起 Stage 时写明）。即：目标是 **P1 需求摸底这个 Phase 剩下要做的事**，分解出**本 Phase 内的后续 Stage**（S3、S4…），不是分解整个项目。

按该层级的四处差异：

| 项 | 本次取值 |
|---|---|
| 目标 | P1 剩余部分——**让客户演示能跑通** |
| 分解出 | 多个 Stage（S3 起，可能是原子或议题） |
| 议题形式 | `S{m}` 或 `S{m}G` |
| 非本级功能的去向 | **本 Phase 内的后续 Stage**——属规划产物，不经延迟需求 |
| 跨单位接口契约的家 | **本 Phase 概况**（[../P1-概况.md](../P1-概况.md)） |

**为何走 devBlueprint 而非直接开工**：用户的新目标（客户面前跑通整个流程）牵动三件互有依赖的事——译名、导航、外部操控机制——且远期还要用 React 全面重写 UI。**三者的次序与边界须先定清**（如"译名必须在导航与按钮文案之前，否则要改两遍"），而这正是 devBlueprint 的职能：定需求图谱 → 定架构 → 定分解路线。

## 当前进度

| Round | 步骤 | 工作项 | 状态值 |
|---|---|---|---|
| R1 | A（shape） | 图谱讨论——针对客户演示的需求讨论 | **已收敛**（19 步；SA 已跑完、13 条待验命题 11 条有判定；Fid 三段核查已完成、11 条发现已修） |
| R2 | B（build） | 需求图谱——把 R1 的讨论结论构建成图谱文档 | **未开始** |

- **当前在**：**R2 B 步（`build`），尚未开工。** A 步已于 2026-09-23 收口。

### B 步的开工信息

| 项 | 值 |
|---|---|
| **play** | `build`（见 [../../流程体系/plays/play-build.md](../../流程体系/plays/play-build.md)） |
| **Spec** | [../../流程体系/workflows/devBlueprint/B-vision-devBlueprint-build.md](../../流程体系/workflows/devBlueprint/B-vision-devBlueprint-build.md) |
| **依据** | [R01-图谱讨论/P1-S2-R1-A图谱讨论.md](R01-图谱讨论/P1-S2-R1-A图谱讨论.md)（19 步，1720 行） |
| **两份产物** | ① `需求图谱.md` → 落 `0-P1-S2文档/`（**跨轮演进，文件名不带 R**）；② `P1-S2-R2-B回执.md` → 落 `R02-需求图谱/`（带 R 号，**状态值与复核建议写在回执里、不写进图谱**） |
| **打tag** | **前** —— 开工前先打 `P1-S2-R2-start`（`git tag P1-S2-R2-start`，只留本地不推远端，见规范 §7.4） |
| **需求编号前缀** | 核心 `CR-` / 重要 `IM-` / 增强 `EN-` / 实验性 `EX-` / 暂缓 `DF-`，各前缀内独立三位序号 |
| **出口** | 状态值 `图谱已产出` → **C 步（架构讨论，shape）** |

**B 步须读的两处**：Spec 的「定稿前自评审」六条（强制，发现问题先改再交付）与「产物规格」的必填/条件节清单。

**⚠ 落档开关已关**（A 步收口，按讨论契约 §7 "谁关"）。B 步是 build 非 shape，主产物不是讨论记录；**若 B 步中途冒出讨论**，按规范 §11.1 就地产 `P1-S2-R2-B讨论记录.md` 并**重开落档开关**指向它。

- **A 步的 Spec**：[../../流程体系/workflows/devBlueprint/A-vision-devBlueprint-shape.md](../../流程体系/workflows/devBlueprint/A-vision-devBlueprint-shape.md)
- **A 步的依据**：用户初始方向（见下方「本 Stage 的输入」），外加 S1 的全部产出。
- **A 步 `打tag=否`**（该 Workflow 步骤表）。
- **产物**：[R01-图谱讨论/P1-S2-R1-A图谱讨论.md](R01-图谱讨论/P1-S2-R1-A图谱讨论.md)（主产物）+ [R01-图谱讨论/P1-S2-R1-A待验表.md](R01-图谱讨论/P1-S2-R1-A待验表.md) + 五份调研报告（弹簧行业资料 / 参考项目 saoxia / zelin 索引 + 其翻译·财税·机制三份）+ `Spike/` 下 14 份探针留档与两份 Fid 核查报告

### A 步已定下的（不再变动）

| 项 | 结论 |
|---|---|
| **定位** | **可信度优先（A）+ 演示编排内置摸底问点**。客户看到的每个界面都是上线后要用的那个；每环节预留"你们厂这里怎么做"的问点 |
| **演示范围** | **S1 已定稿的 23 环节最小闭环操作稿**（用户明确裁决）。质检/外协/月结不演示，改现场口头询问 |
| **范围分解** | 判为**单目标**（外部操控机制在本次只做到"够演示用"一档） |
| **核心需求（8 条）** | 18 弹簧行业调研 / 1 译名全量准确 / 2 导航按业务流重排 / 3 树形 DocType 不空白 / 4 工艺损耗反算投料量 / 5 完工入库费用科目默认值 / 6' 真实弹簧产品闭环样本（A/B 两套）/ 8' 演示脚本驱动的最小操控机制（须可从任一节点起跑） |
| **重要（3 条）** | 10 客户可读版演示材料 / 13 演示脚本文本 / 14 内置摸底问点清单 |
| **增强（2 条）** | 11 拼音检索 / 17 演示环境可重置性 |
| **实验性（2 条）** | 19 演示录屏与自动记录 / 22 移动端查看 |
| **暂缓（2 条）** | 21 计件工资演示（hrms 未装，转摸底问点）/ **20 弹簧参数结构化字段**（改用国标型号标记法，零开发） |
| **转演示纪律不进开发** | 7 报工工时（**用户改判：是落地流程控制非软件缺陷**）。样本工时须合理 |

### 两条次序约束（E/F 步排路线时必须遵守）

1. **第 4、5 项服务端修正必须在第 6' 项建样本数据之前**——工艺损耗不反算、费用科目须手填这两个缺陷会让建出来的数据本身是错的（工单短交 250 支、费用科目进损益表）。改完再建只建一次，反序要建两次。
2. **V-01 的结论必须在建演示公司之前拿到**（已拿到，判 `go`）——公司一旦开始记账，科目表不能换。第 12 项与第 6' 项在"用哪套科目表建公司"上**互斥**。

### 待验表状态（9 条，8 条已填）

| 编号 | 主题 | 判定 |
|---|---|---|
| V-01（阻塞） | 中国本地化能否 app+hook 化、不改 erpnext 本体 | **`go`** —— `regional_overrides` hook，20 个 `@allow_regional` 覆盖点，分派实测生效 |
| V-02 | 中国科目表整理工作量 ≤3 人日 | `无法判定`（人日不可测）。但测出**重组后 `root_type` 只需给顶层 5 处而非 82 处**；拿现状直接建公司是硬失败 |
| V-03 | 自制科目表模板能否进建公司下拉 | `go`（`override_whitelisted_methods` 实测生效）。**但"点了能用"未跑**——`get_chart()` 不经 `execute_cmd` |
| V-04 | 中国增值税能否用原生 Tax Template 表达 | `go`，三项实跑通过。**反直觉点**：手填 `item.item_tax_rate` 无效，会被 `update_item_tax_map()` 覆盖，必须走真的 `Item Tax Template` |
| V-05 | 专票/金税测试环境是否需企业资质 | `go`——**不需要**（诺诺沙箱对未签约方开放）。**推翻了 Claude 原预判** |
| V-06 | 中国财报格式能否用原生报表配置产出 | `go`。v16 新增 `Financial Report Template` 声明式引擎。**但只有资产负债表实建跑通**，现金流量表未验 |
| V-07 | `PH-P1002` 其余四例是否同源 | **`no-go`**——四例分属三条路径，分组 {1,5}/{2}/{3}/{4}，**不能修一处同修** |
| V-08 | saoxia/erpnext_china 可否借用 | **`no-go`**——无任何财税本地化、只适配 v15、许可证矛盾、含 33727 个真实手机号 |
| V-09 | zelin-tech/erpnext_china 可否借用 | **待验**（首次派出遇 API 超时、产出为零，已重派；重点查其 v16 翻译与是否用 `context`） |

### 本 Round 待决项

| # | 待决项 | 依赖 | 状态 |
|---|---|---|---|
| 待决-1 | `Reference/` 两仓的留删（含 saoxia 的 PII 处置） | 全部 Agent 回来 | 用户裁决"先留着，等全部回来再定" |
| 待决-2 | 第 12 项中国本地化的分级 | V-09 | 等 |
| ~~待决-3~~ | ~~第 20 项弹簧参数字段分级~~ | — | **已清**（定为暂缓，用国标型号标记） |
| 待决-4 | 第 3 项树视图空白的**根因复核** | 已派出（`Spike/V10-*`） | 跑 |
| 待决-5 | V-07 例 2（侧栏卡片缺失）的定级 | 依赖第 2 项导航重整范围 | 留待 C/E 步 |

### ⚠ A 步暴露的、须带往下游的事

| # | 事 | 去哪 |
|---|---|---|
| 1 | **S1 对 `LG-063`（报工工时）的定性须改**——用户裁决它是操作前提非软件缺陷，故"演示三个雷"应改为**两个雷 + 一条操作前提** | B 步落图谱时 |
| 2 | **V-07 例 2 性质已变**：侧栏卡片缺失是 v16 架构变更下的功能缺失（`add_card()` 全仓零调用者、卡片机制是死代码），**刷新也永不出现**，不属"不刷新即不显示" | B 步落图谱时重新归类 |
| 3 | **V-07 例 4 写入侧在 Python**：`job_card.py:680-685` 用 `db_update()` 旁路插库、绕过 `self.save()` 的校验与 idx 重排。只修读取侧则空行仍入库 | C 步定改法 |
| 4 | **译名无现成共识可依**：`Reference/` 两仓的 `zh.csv` 对同一词译法冲突（Work Order：生产订单 vs 生产工单；BOM：BOM vs 物料清单），saoxia 那份内部还自相矛盾。**目标侧标准须本项目自己定** | C 步定译名落地形态 |
| 5 | **`module/custom/{doctype}.json`（Customize Form 内置机制）比 fixtures 导出更适合 git 管理**（V-08 所得）。**⚠ 依据已缩水**：V-09c 查明 zelin **两种都没用**（其 `fixtures/` 是死文件、Custom Field 靠 `after_install` 代码 insert），故该对比**只有 saoxia 一个样本，不是两个项目的共识** | C 步定译名与导航的落地形态 |
| 6 | **`12Cr18Ni9` 不对应 `SUS304`**（对应 SUS302/AISI 302；SUS304 对应 `06Cr19Ni10`） | 建样本数据时 |
| 7 | **压簧/拉簧参数表是 B 级且同源页面内部有冲突**（d=5/D=30：661 vs 962 等），**建样本前须复核实际要用的那几行**（与独立来源比对或用刚度公式反算） | 建样本数据时 |
| 8 | **五个管理侧环节（接单/排产/领料/入库/发货）无行业公认车间叫法**，工艺资料只覆盖车间制造段 | 第 14 项摸底问点清单须含这五个环节的叫法询问 |
| 9 | **"三张财报都能出"目前只有资产负债表有据**。**⚠ 已进一步改判**：V-09b 实证 zelin **把现金流量表做成可提交单据（DocType）而非 Report**，从 GL Entry 按现金银行科目拉流水（直接法正确做法）→ 故 **V-06 的 `go` 对现金流量表不成立**，它不是原生报表配置能产出的，**须自建一套**（该结论已随 DEC-035 进核心需求范围） | C 步定财报方案时 |
| 10 | **站点已恢复 `20260922_145623`**，不再是"只有骨架"的中间态。项目概况与本概况里"要完整闭环样本须先 restore"的表述已失效 | Stage 收口时按常驻文件契约逐出 |
| 11 | **socketio 端口错位已修，但 `S1 全程实操都在 realtime 死的环境下做的`**——站点配置 `socketio_port` 9000 vs compose 映射 `9100:9000`，而 9000 落在 Windows 保留段（实测排除范围含 8995-9094）。已两头同挪 9100 并由用户实证（两标签表单自动同步）。**修法与自查已写进 `docker/README.md`** | **B 步落图谱时须重估 `PH-P1002`**——V-11b 未验：那四例是否因 realtime 断连而生 |
| 12 | **核心需求第 3 项（树形 DocType 不空白）已降为回归检查项**——浏览器实点当前不复现，R6 判的"必然"被现象否证；但 R3 观察是真实记录、触发条件仍未查清，且**导航重整会改动侧栏链接生成、可能重新引入** | B 步落图谱时按"回归检查项"写，不按"必然缺陷"写 |
| 13 | **译名工作量有了实测量级**：V-09a 机械扫出同类撞名（同一目标词 ≥2 源词）**共 914 组**；官方 v16 中文翻译的四条已知缺陷中，**缺陷 2、3 已核实是从 `erpnext/locale/zh.po` 继承**（`:2244`/`:15827`/`:33973`） | C 步定译名方案时——这是排工作量的依据 |
| 14 | **`context` 参数解不了"多源词撞一目标词"**（Claude 原判读被 V-09a 推翻）——`context` 是按 source+场景**拆分**译文，而撞名是多个源词撞到同一目标词，**只能改其中一方的用词** | C 步定译名方案时。**第 12 步③立的那条判读规则已作废** |
| 15 | **中国科目表的同名父子结构会致选错科目，且改译名解决不了**——zelin 的 4 张表中 3 张有同名父子（一般企业2024 **13 处**含 `应收账款 > 应收账款` 11220/11221）。**不会触发官方那种追加数字 `1`**（`add_suffix_if_duplicate` 去重键含科目号），**症状变成"只差科目号前缀"**（`11220` 是 group、`11221` 才能记账） | C 步定科目表方案时须单独处理（如给 group 层加后缀） |
| 16 | **做中国本地化的两条硬约束**（从 zelin 四处配置层缺陷反推）：① **科目号引用必须有机械校验**（遍历科目表验证每个引用存在）——zelin 缺这道，致其 `一般企业会计准则(2024)` 示例配置 144 个引用**命中 0 个**；② **不得把配置装载包在裸 `except` 里只写 log**——zelin 四个 setup 函数全是这写法，**后果是建完公司看不出问题、做账时才发现** | C 步定中国本地化架构时 |
| 17 | **V-12 已查明**（Claude 自验）：侧栏 `/undefined` 404 的根因是上游 Frappe v16 **`icon_html` 有写入点、无读取点**（`sidebar_header.js:145`/`:160` 赋值，而 `:351-375` 的模板只读 `icon`/`icon_url`，全仓 `icon_html` 零读取点）。功能不受损、仅破图 + 每项 1 个 404 | C 步定改法时（牵涉"改上游还是 app 侧覆盖"，而**前端 js 的覆盖机制本 Round 未验**） |

## 本 Stage 的输入（S1 移交）

### 一、用户已裁决的方向（S1-R6 第 9、10 步，DEC-020~022）

**最优先需求（用户原话）**：**在客户面前跑通整个流程。**

三件事按序做，**次序本身是有理由的，不能重排**：

| # | 事 | 为何在这个位置 |
|---|---|---|
| 1 | **译名准确稳定** | 用户要求"不应该出现有些翻译了有些没翻译的情况"。**必须最先**——导航重整与按钮文案都要用到译名，反序会改两遍 |
| 2 | **导航重整** | 按业务流（接单→排产→领料→报工→入库→发货→开票→收款）重排入口。**前置**：LG-061 那个实测（改 `standard=1` 记录会不会被 `bench migrate` 覆盖）决定是"改记录（1 天）"还是"出 fixtures（3 天）" |
| 3 | **外部操控编排机制** | `PH-P1003`。供演示脚本 / MCP / React 前端三者共用，演示只是第一个用例 |

**远期**：全面重写 UI，**技术栈 React**。已查明**与近期工作无技术债传递**——desk 是 jQuery 不是 Vue，自建前端走 REST API v2，两套独立。

### 二、未裁决、A 步须处理的三件事

| # | 待定 | 选项已摆出 |
|---|---|---|
| 1 | **"界面自己动"走甲还是乙** | 甲=服务端推事件驱动（Claude 推荐：真正的外部操控、MCP 可复用、可拿手机当遥控器；代价是约百行前端监听）；乙=浏览器侧驱动（略简单，但控制权仍在 desk 内，MCP 与 React 复用不到驱动界面那部分） |
| 2 | **译名的目标侧术语标准** | **这是硬前置且须用户参与**（LG-070 缺口四）。缺它则清单补全了也定不了名。可选范围：先只统一"收付款方向"与"撞名"两类最确定的，行业术语留到实地考察后 |
| 3 | **演示要覆盖哪些环节** | 八步线性流程是用户举的例；但 S1 已知**质检、外协、月结完全未覆盖**，演示要不要包含它们未定 |

### 三、演示的三个已知雷（A 步须逐条定处置）

| # | 雷 | 后果 | 出处 |
|---|---|---|---|
| 1 | **工艺损耗不反算投料量** | 每张按 SO 生成的工单**必然短交**（SO 5000 支 + 5% 损耗 → 完工 4750）。"一路下一步"走到排产就会暴露，**UI 层补不了，要改服务端 mapper** | LG-039 / S1 概况 |
| 2 | **报工工时若填成默认或极小值，毛利率虚高 40 个百分点** | R3 实测工序成本记 0.27 元而非 433.3 元、毛利率 68.8% vs 应为 28.8%。**客户会看到漂亮但错误的成本数字，比不演示更糟** | LG-063 |
| 3 | **完工入库的额外费用科目须逐行手填**（N = 工序数 × 成本构成数，实测 11 行） | 演示时手填 11 行不可接受，且填错即成本进损益表 | LG-014 / `Company.expenses_added_to_stock_account` 出厂为空 |

### 四、S1 的可复用资产

| 资产 | 状态 |
|---|---|
| [最小闭环操作稿.md](../S01-场景摸底/0-P1-S1文档/最小闭环操作稿.md) | **已定稿**（R4），23 环节全跑通。**演示流程的现成脚本底本** |
| L0-L2 三篇教材 | 段②④⑤ **继续有效**（不依赖 UI）；**段①（操作路径）待导航重整后重写** |
| 52 场景 / 9 域的判定（✅28 🔧13 ⚠10 ❓1） | 有效，但 ⚠ 项的排序未完成（余 4 项未读源码） |
| 8 张译名对照表 | **三张核心表经独立重跑确认 29/29 准，可直接采信** |
| 译名改造技术路径 | 全查清有源码行号（`translate.py:135-240` 五级优先级、A+B 叠加、`context` 机制、清缓存命令） |
| 站点备份三个基准点 | `20260922_145623`（R3 闭环完整数据，另存 `保留-R6重装前/`）可随时恢复作演示样本 |

### 五、当前站点状态（**不是完整样本，注意**）

**R6 重装后站点处于"L0 走到第三步"的中间态**：

| 项 | 值 |
|---|---|
| 公司 | `华东弹簧`（HDS），95 科目 / **5 仓库** / 2 成本中心 |
| 财年 | **0 条** |
| 默认公司 | **空** |
| `expenses_added_to_stock_account` | **None** |
| 业务数据 | **无**（0 Item / 0 BOM / 0 单据 / 0 GL / 0 SLE） |

**要完整闭环样本须先 `docker/restore.sh`** 恢复 `20260922_145623`。

## 本 Stage 的长效信息

（本 Stage 内跨 Round 有效、Stage 收口即失效的信息。）

**本次规划层级：Phase 级**——见「当前编排」节。E/F 两步取"Stage"作分解单位。

### A 步产生的（R1，五条）

**一、两条次序约束（E/F 步排路线时必须遵守）**

1. **服务端修正必须在建样本数据之前**——工艺损耗不反算投料量、完工入库费用科目须手填这两个缺陷，会让建出来的样本数据**本身就是错的**（工单短交 250 支、费用科目进损益表）。**改完再建只建一次，反序要建两次。**
2. **V-01 的结论必须在建演示公司之前拿到**（已拿到，判 `go`）——公司一旦开始记账，**科目表不能换**。故中国本地化与样本数据在"用哪套科目表建公司"上**互斥**。

**二、建样本数据前须复核参数（不是"最好做"而是"不做会把错数字放进演示"）**

压簧/拉簧的国标参数表是 **B 级来源**且**同源页面内部有冲突**（d=5/D=30：661 vs 962；d=8/D=50：1154 vs 2203——摘要疑为跨行错位，该站表格系 OCR 转录、有已知错字）。**只需复核实际要用的那几行，不必复核全表。** 手段：与另一独立来源比对，或用弹簧刚度公式反算校验。

另：`12Cr18Ni9` **不对应** `SUS304`（它对应 SUS302/AISI 302；**SUS304 对应 `06Cr19Ni10`**）。填材料字段时别踩。

**三、中国科目表的同名父子结构会致选错科目，且改译名解决不了**（C 步定科目表方案时用）

zelin 的 4 张中国科目表里 **3 张有同名父子**（一般企业准则2024 **13 处**，含 `应收账款 > 应收账款` 11220/11221）。

**关键差别**：它**不会**触发官方那种"子科目被追加数字 `1`"——`add_suffix_if_duplicate` 的去重键是 `account_number + " - " + name.lower()`，科目号不同故键不冲突。**症状因此从"只差一个数字 1"变成"只差科目号前缀"**：下拉里 `11220 - 应收账款` 是 group、`11221 - 应收账款` 才能记账。

**故第 1 项（译名）解决不了这个风险，须在科目表设计时单独处理**（如给 group 层加「（汇总）」这类后缀）。

**四、做中国本地化的两条硬约束**（从 zelin 四处配置层缺陷反推，C 步定架构时用）

1. **科目号引用必须有一道机械校验**——遍历科目表验证每个被引用的科目号存在。zelin 缺这道，致其 `一般企业会计准则(2024)` 的示例配置 **144 个引用科目号命中 0 个**，且利润表示例的税金明细行**系统性错位 7 处**。
2. **不得把配置装载包在裸 `except` 里只写 log**——zelin 的 `setup_tax_template`/`setup_tax_rule`/`set_default_accounts`/`set_item_group_account` 全是这写法，**后果是建完公司看不出问题、做账时才发现**。

**五、~~`context` 判读规则~~（已作废）**

第 12 步③曾立："用了 `context` → 其翻译可当基线；没用 → 那两条撞名缺陷必然也没解决"。**该规则第 17 步①已作废**——`context` 是按 source + 场景**拆分**译文（解决"同一源词多义"），而撞名是**多个源词撞到同一目标词**，只能改其中一方的用词，`context` 帮不上。**保留此条是为防后续重新捡回这个错判读。**

## 本 Stage 延迟需求登记册

（列：编号 | 需求描述 | 来源 | 登记日期 | 唤醒条件 | 原始记录 | 状态）

暂无。本 Stage 内新提出的条目在此登记（编号 `SH-P1S2001` 起），Stage 收口时逐条判做了没有，未做的换发 `PH-` 号推进 [../P1-概况.md](../P1-概况.md) 的本 Phase 登记册。

**注**：S1 上交的 9 条已在本 Phase 概况登记册（`PH-P1001` ~ `PH-P1009`），**不在本表重复**——其中 `PH-P1001`（译名）、`PH-P1002`（前端刷新）、`PH-P1003`（外部操控机制）三条的唤醒条件已触发（客户演示前必做），本 Stage 的规划须把它们排进 Stage 序列。
