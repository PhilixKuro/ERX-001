# P1-S2-R6 · devBlueprint D 步（`build`）回执

**产出步骤**：P1-S2 演示蓝图 · Round 6 · devBlueprint **D 步**（`build`）
**唤起方**：用户「继续开发项目」→ 流程中枢；D 步由 R5 SB 步出口（状态值 `修补已落地`）路由而来
**依据**：[P1-S2-R4-C架构讨论](../R04-架构讨论/P1-S2-R4-C架构讨论.md)（第 1–19 步）＋ [P1-S2-R4-C待验表](../R04-架构讨论/P1-S2-R4-C待验表.md)（V-14…**V-19**）＋ [需求图谱](../../0-P1文档/需求图谱.md) v1.1
**规划层级**：Phase 级
**打tag**：`P1-S2-R6-start`（D 步 `打tag=前`，**本地未推**）
**日期**：2026-09-25

**两份产物**（落点按 `流程规范.md` §11.1）：

| 产物 | 落点 | 状态 |
|---|---|---|
| [架构文档](../../0-P1文档/架构文档.md) v1.0（276 行） | Stage 外的 `0-P1文档/`，文件名不带 R 号 | 已产出 |
| **12 份 ADR**（ADR-0001…0012） | 同上 | 已产出，全部 `accepted` |

---

## 逐项执行结果

| 任务 | 对应依据 | 落地位置（文件:行） | 结果 | 完成时间 | 说明 |
|---|---|---|---|---|---|
| T-001 | C 步 12 条决策 DEC-052…063 转写为 ADR | `0-P1文档/ADR-0001…0012-*.md` | 完成 | 2026-09-25 | 一一对应，无合并无拆分 |
| T-002 | 架构文档 · 文档头部＋版本变更记录 | [架构文档.md:1-31](../../0-P1文档/架构文档.md#L1-L31) | 完成 | 2026-09-25 | 版本 v1.0；关联决策表 12 行 |
| T-003 | 架构文档 · 架构总览 | [架构文档.md:35-39](../../0-P1文档/架构文档.md#L35-L39) | 完成 | 2026-09-25 | 一段，含「末位装载是架构基石」这条 |
| T-004 | 架构文档 · 技术栈选型 | [架构文档.md:41-58](../../0-P1文档/架构文档.md#L41-L58) | 完成 | 2026-09-25 | 12 行，每行挂关联决策或标「上游」 |
| T-005 | 架构文档 · 模块划分（6 个单位，每个 5 项齐全） | [架构文档.md:60-168](../../0-P1文档/架构文档.md#L60-L168) | 完成 | 2026-09-25 | 1 模块 + 3 普通包 + 2 json 目录；含单位约定与规模自查 |
| T-006 | 架构文档 · 接口双向对账表 | [架构文档.md:154-167](../../0-P1文档/架构文档.md#L154-L167) | 完成 | 2026-09-25 | 7 对，6 对齐、1 对经本步补齐（见复核建议 2） |
| T-007 | 架构文档 · 前瞻性设计清单 | [架构文档.md:169-183](../../0-P1文档/架构文档.md#L169-L183) | 完成 | 2026-09-25 | 3 条，全部锚到真实需求编号；1 条有意排除并说明 |
| T-008 | 架构文档 · 两层架构图 | [架构文档.md:185-274](../../0-P1文档/架构文档.md#L185-L274) | 完成 | 2026-09-25 | 第一层系统与外部交互；第二层 `erx_core` 及六类接入点 |
| T-009 | **V-19 实测（SA 附属探针）→ 背书 ADR-0007** | [P1-S2-R4-C待验表.md:17](../R04-架构讨论/P1-S2-R4-C待验表.md#L17) + 同文件末 `### V-19` 节 | 完成 | 2026-09-25 | 判定 **`go`**；子 Agent 产出，产物沿用 C 步字母填回 C 待验表 |
| T-010 | 据 V-19 新事实回改架构文档三处 | [架构文档.md:82-91](../../0-P1文档/架构文档.md#L82-L91)／`:265-267`／`:274` | 完成 | 2026-09-25 | §3.1 四处缺口表；接入点⑤默认科目范围六→一；三处风险行 |

---

## 一致性核对

| 核对项 | 方法 | 结果 |
|---|---|---|
| ADR 编号齐套、无重号跳号（自评审①） | 文件名取号排序比对 1…12 | **1–12 连续无缺**，12 份文件 |
| 架构文档内部链接可解析 | 抽全部 16 条 `](...)` 逐条 `os.path.exists` | 15 条解析，**1 条为本回执自身**（写此文件前必然不存在） |
| ADR 引用的编号真实存在（自评审①后半） | ADR-0007 的 18 个编号逐个比对需求图谱＋C 讨论＋C 待验表 | **未解析 0 个** |
| 每份 ADR 五节齐全＋状态标注 | 12 份逐份查 `## 上下文/决策/后果/备选方案/关联` 与 `状态：` | 12 份全齐，**全部 `accepted`** |
| 模块五项齐全（自评审④前提） | 6 个单位逐个查 职责／对外接口／依赖／所属分解单位／关联决策 | 6 个全齐 |
| 前瞻性设计锚点真实存在（自评审③） | EX-002／IM-003／IM-010／IM-011 在需求图谱 v1.1 内 grep | 4 个编号**全部命中** |
| 前置依赖版本号相符 | 架构文档头部声明 v1.1 vs 需求图谱自身头部 | **相符** |
| §11.1 落点合规 | 查 `0-P1文档/` 内有无「状态值」「复核建议」节；查文件名带不带 R 号 | 均无，文件名不带 PSR；PSR 仅作指针出现 |

---

## 自评审执行结果

> D 步 Spec 的「全量验证」节改为本节：**本步产物是文档，无测试门**。六项自评审逐项报执行方法与结果，**不是逐项声明「已做」**。

**① 决策记录齐套（编号无重号跳号，引用的决策编号真实存在）—— 通过**

- 编号：`ADR-0001`…`ADR-0012` 十二份，取号排序后与 `range(1,13)` 比对，**无缺号、无重号**。
- 引用：ADR-0007 的 18 个编号（CR-005/006/007、DEC-058、IM-007、LG-101/102、NV-037/038/050、V-18/19、ADR-0001/0003/0004/0005/0007/0012）逐个比对需求图谱 v1.1、C 架构讨论、C 待验表三份来源，**未解析 0 个**。ADR 类编号另核对应文件确实存在。
- 架构文档全部 16 条链接逐条查存在性：15 条解析，第 16 条是本回执自身。

**② 接口双向对账（单向声明即为缺口）—— 通过，但经本步补齐一处**

7 对声明逐对核「被依赖方的『对外接口』是否真的列出该接口」。6 对直接对齐。**第 7 对原为缺口**：接入点⑥ 的 `after_migrate` 自检声明依赖「`cn_tax` 的建账结果（科目数 + `root_type`）」，而 `cn_tax` 的「对外接口」列的是 handler 与 DocType ——**建账结果是它的产出状态、不是它声明的接口**，属单向声明。**本步补齐**：`cn_tax` 须显式提供一个「建账结果自检函数」供接入点⑥调用，已计入 §3.1 对外接口⑤的入口函数族。**这是本步对账里最勉强的一处**，见复核建议 2。

**③ 前瞻性设计锚定到真实需求编号 —— 通过，但一条锚点偏弱**

3 条（P-1／P-2／P-3）逐条在需求图谱 v1.1 内 grep 锚点编号：EX-002、IM-003、IM-010／IM-011 **全部命中**。第 4 条候选（desk js 覆盖登记表）**锚不上任何需求编号**，按 Spec「不允许悬空预留」**不进清单**，改归 ADR-0009 的组成部分，并在文档内显式列出以防误判为漏记。**P-1 的锚点偏弱**，见复核建议 3。

**④ 模块职责单一 —— 通过**

6 个单位的「职责」逐条读，判据是**一句话内有无「和」「以及」连接两件不同的事**：`cn_tax`「按中国会计准则出账与出表」（出账与出表是同一准则下的一件事）／`i18n/`「维护译名的单一来源，并断言它没有被第二来源污染」（断言是维护的手段，非第二职责）／`patches_mfg/`／`demo/`「承载演示操控与演示数据生成这两件共享同一套建单机制的事」（**这条显式交代了它承载两件事及其共享理由**）／`workspace_sidebar/`／`desktop_icon/`。**另有一处已登记的拆分信号**：`cn_tax` 对外接口 16 项，命中 C 步「公开接口超过 10 个」这条检查，**当前不拆**并写明将来拆的触发条件（报表继续增加时把「报表」独立成第二模块）。

**⑤ 规模自查（>10 模块先自问）—— 通过，未触发**

6 个单位（1 模块 + 3 普通包 + 2 app 级 json 目录），**低于阈值 10**。且此处的「模块」有更严的口径：`modules.txt` 每多一项就自动生成一个 hammer 侧栏进 boot，**模块数是界面成本**，故 `modules.txt` 实际只列 **1** 个。

**⑥ 状态标注正确（被否/被取代的状态已更新、内容保留、取代关系两侧写明）—— 通过（无取代关系）**

12 份 ADR 全 grep `supersede`／`取代`／`deprecat`／`superseded`：**无一份有取代或废弃关系**（「取代：无」是 12 份「关联」节的一致写法）。本步是首次产出，12 条决策全部现行，故本项**无实质对象可核**，据实报告为「trivially 满足」而非「已核过取代两侧」。**被否决的 21 条 NV 不是 ADR**，它们作为「备选方案」节的内容保留在对应 ADR 里（如 NV-037/038/050 全在 ADR-0007），符合「内容保留」的要求。

**一处不在六项之内、但本步做了的验证**：**V-19 实测背书 ADR-0007**。DEC-058 的 A 案在 C 步定案时**零实测**（V-18 刻意排除了它），若不验就写 `accepted`，该 ADR 从落笔就可能是错的。本步派子 Agent 跑 V-19，判定 **`go`**，并据其新事实回改架构文档三处（见 T-010）。**这不是自评审项，是 D 步产物正确性的前提。**

---

## 偏离与暂停

| # | 偏离项 | 原定 | 实际 | 理由 |
|---|---|---|---|---|
| 1 | **D 步中途插入一次实测** | D 步是 `build`，只转写不验证 | 开工前先派子 Agent 跑 V-19（SA 附属探针），判定 `go` 后才写 ADR-0007 | C 步复核建议明写「D 步开工前补一次实测」，用户 2026-09-25 指名派出。**属附属形态可选步骤，不另占 Round**，产物沿用 C 步字母填回 C 待验表（`流程规范.md` §7.1／§11.1） |
| 2 | **架构文档产出后又回改三处** | 一次写成 | §3.1 补四处缺口表、接入点⑤范围六→一、三处风险行更新 | V-19 查出 C 步未掌握的事实（A 案已修对五个默认科目、zelin 两处源码缺陷、flag 无复位点）。**不回改就会把已被证伪的判断写进产物** |

**暂停项**：无。

---

## 新增约定

| # | 约定 | 落点 | 来源 |
|---|---|---|---|
| 1 | **`after_install` 与 `hooks.py` 不写业务逻辑，只做接线** —— `after_install` 按序调用 `cn_tax` 内的入口函数，`hooks.py` 的 handler 路径指向 `cn_tax` 内的函数 | 架构文档 §3.1「一处本步裁定」 | **本步裁定**（C 步复核建议第二节点名交给 D 步的收拢办法，本步采纳） |
| 2 | **`demo/` 共享的是「建单辅助函数」，不是「节点定义」** —— 演示操控与数据生成共用同一套建单机制，但节点清单是操控侧独有 | 架构文档 §3.4 | **本步裁定**（C 步只说「共享同一套机制」，未说共享到哪一层） |
| 3 | **`cn_tax` 须提供「建账结果自检函数」** | 架构文档 §3.1 对外接口⑤、§3.8 第 7 行 | 本步接口双向对账的补齐动作 |
| 4 | **IM-007 的 `after_migrate` 自检要分两条**：`[-1]` 归属断言守派发层（下拉框/科目树），**守不住建账路径**；建账侧须另加「建公司后断言科目数与六个 `root_type`」 | ADR-0007「关联」节、架构文档 §5.2 接入点⑥ | C 步第 14 步已指出，本步落成文字 |
| 5 | **并入式抄 zelin 源码时要修掉 6 处 `log_error` 误用并补 `account_category`，修改点登记进 app README** | 架构文档 §3.1 缺口表第 4 行 | V-19 实测查出 |
| 6 | **建公司前须显式复位 `ignore_chart_of_accounts`**，不依赖进程边界 | 架构文档 §3.1 缺口表第 3 行 | V-19 实测查出 |

---

## 未做项

| # | 未做 | 原因 | 归属 |
|---|---|---|---|
| 1 | **跨单位接口契约未定稿** | devBlueprint 的分工是 **D 识别 / F 定稿**。本步已识别（六类接入点 + §3.8 对账表），定稿在 F 步 | F 步 |
| 2 | **LG-101（HRMS 同钩子交互）仍未验** | 本 bench 未装 hrms，**用户明确指示本次不为此装 app**。A 案把三钩子挂 `Company` 正是这条风险的落点 | **装 HRMS 后须重验 V-19 第三段**，已写进 ADR-0007 失效条件 |
| 3 | `existing_company` 分支零实测 | 本项目无父子公司/集团架构场景 | 若将来要建集团架构，见 ADR-0007 失效条件 |
| 4 | 另两份中式科目表未验 | V-19 只验 `小企业会计准则(2024)` | 若改用另两份，须重验 `account_category` 缺陷是否触发 |
| 5 | **未做任何 git 提交/推送** | CLAUDE.md「版本管理操作规则」：默认不落库。仅 `P1-S2-R6-start` tag 属流程步骤 `打tag=前` 的例外，且**本地未推** | 待用户显式许可 |

---

## 状态值

| 判据 | 实际 | 判定 |
|---|---|---|
| 架构文档与全部 ADR 已产出、六项自评审全过 | 架构文档 v1.0（276 行）＋ 12 份 ADR 全 `accepted`；六项逐项执行并报了方法与结果 | ✅ |
| 阻塞 ADR-0007 的 V-19 已有判定 | **`go`**（子 Agent 实测，四项边界三项通过，第四项的缺陷已转成 §3.1 缺口表） | ✅ |
| 是否有未解决的阻塞项使本步产出不可用 | 无。LG-101 未验但**不阻塞**——它是已登记的失效条件＋已备退路（NV-050/LG-102），非当前缺口 | ✅ |

**状态值**：**`架构已产出`**

**出口**：按 devBlueprint 出口路由，`架构已产出` → **E 步**（`shape`，路线讨论）。

---

## 复核建议

> 按 D 步 Spec 的四类写。**不是「请全面复核」，是指名哪几处最该看。**

**1. 决策记录与讨论的差异处**

- **ADR-0007 的内容大幅超出 C 步讨论**。C 步定案 A 时手里只有「zelin 上游在用」这一条依据；本步的 ADR-0007「正面/负面」两节**几乎全是 V-19 新事实**（266 条逐项一致、五个默认科目被修对、四处缺口、两处 zelin 源码缺陷、flag 无复位点）。**请核这些新事实有没有改变「取 A」这个决策本身的成立性**——本步判断是没有（缺陷都可规避且已列出补法），但这是本步的判断，不是 C 步的。
- **一处明确更正了 C 步的判断**：C 步在 P1-S2 概况开发纪律 6 里写「**A 案同样会踩**」（指默认科目挑错），V-19 证明**A 案不踩**，`set_company_default` 修对了五个，只漏 `round_off_account`。已在架构文档 §3.1 写明此处更正。**概况那条纪律的文字本步未改**（它在 Stage 概况里，不是本步产物），请复核是否要同步改。
- **ADR-0002 与 ADR-0004 的「备选方案」节没有被否决的备选**。C 步这两条决策是直接定的，没记 NV 编号。**本步据实写「无」，没有为了格式完整而编造备选方案**。
- **本步自行裁定了两处 C 步留白**（见「新增约定」1、2）：`after_install`/`hooks.py` 只做接线；`demo/` 共享的是建单辅助函数而非节点定义。**这两处是本步填的，C 步没定**，请确认与原意相符。

**2. 接口双向对账里最勉强的一处**

**§3.8 第 7 行：接入点⑥ ← `cn_tax` 的建账结果。** 这对原本是单向声明——`after_migrate` 自检要读「科目数 + 六个 `root_type`」，但 `cn_tax` 的对外接口列的是 handler 与 DocType，**「建账结果」是产出状态、不是它声明的接口**。本步的补法是让 `cn_tax` 显式提供一个「建账结果自检函数」。**勉强之处在于**：这个函数是本步为了让对账表对得上而新增的，C 步没有它；且它与 IM-007 原定的「断言 4 个覆盖位的 `[-1]` 归属」是两件不同的事（一件守派发层、一件守建账路径），**IM-007 这条需求实际上被本步拆成了两条断言**。请复核这个拆分是否该回写进需求图谱。

**3. 前瞻性设计的锚定（哪条是本步自行判断对应上的）**

**P-1（事件协议与业务代码解耦）锚 EX-002 是本步自行判断的，且这条锚点偏弱。** 三重弱：① **EX-002 是实验性需求**，其评估触发条件是「演示成功一次之后，若要复用演示过程、或需远程展示」——**它本身可能不做**；② P-1 的**真实支撑理由是「远期 React 全面重写」，而那条在需求图谱里没有需求编号**（它记在项目概况的技术栈远期计划里，不是一条需求）；③ 故取 P-1 的实际理由不是锚点强，是**成本接近零**（CR-008 无论如何要设计事件协议，把它设计成不依赖 desk 细节不额外花钱）。**按 Spec「不允许悬空预留」，本条锚点形式上合规但理由偏弱，请复核是否该降级或把 React 重写补成一条需求。**

**4. 拿不准处**

- **V-19 的 `go` 判定边界，探针自己也交了出来。** 四项边界里第三项（错误路径）在「同进程内先建过中式公司」时**当场崩、公司整体回滚**。探针判 `go` 的理由是命题两问都有确切答案、且崩发生在配置本就不该出现的组合上（`country='China'` 却选 `Standard`），并明写「**若复核人认为『同进程连续建公司会崩』本身就够触 `no-go`，本探针不反对**」。**本步接受了 `go`**，把那两处缺陷转成 §3.1 缺口表第 3、4 行（要自己修）。**这个接受是本步做的裁量，请复核。** 若复核认为该判 `no-go`，ADR-0007 的状态要改，且要回 C 步重开决策 5。
- **`cn_tax` 的 16 项对外接口没拆。** 命中 C 步「公开接口超过 10 个」这条拆分检查。本步不拆的理由是这 16 项同属一个内聚职责（按中国准则出账出表），并写明将来拆的触发条件（报表继续增加）。**这是本步在「职责内聚」与「接口数阈值」之间选了前者**，请复核。
- **自评审⑥ 无实质对象。** 12 条决策全部现行、无一条被取代或废弃，故「取代关系两侧写明」这项**没有可核的对象**。本步据实报告为 trivially 满足，**没有把「无对象」写成「已核过」**。
- **`log_error` 误用 6 处只有 1 处有实测栈。** 另 5 处（zelin 原文件 173／189／208／227／245 行）是同一写法、读码得出。§3.1 缺口表要求「修掉这 6 处」是按同一写法推的，**其中 5 处未各自触发过**。
- **架构文档的行号引用会随后续轮次改写而失效。** 本回执正文里的 `架构文档.md:NNN` 是本步落笔时的行号；该产物**跨轮就地改写**（§11.1），后续轮次改动后这些行号不再准。查证时请按节号（§3.1 一类）找，不要按行号。
