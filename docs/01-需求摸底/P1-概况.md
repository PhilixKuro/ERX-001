# P1 需求摸底 — Phase 概况

> Phase 级概况：级联指针（→ 当前 Stage）+ 本 Phase 开发范围 + 架构变更（增量）+ 延迟需求登记册。

## 级联指针

- **当前 Stage**：S1 场景摸底（进行中，R2 已落地，下一步由用户实操跑通最小闭环）→ [S01-场景摸底/P1-S1-概况.md](S01-场景摸底/P1-S1-概况.md)
- 该 Stage 绑的是**一次自由编排**（非 Workflow）

## 本 Phase 开发范围

**双重目的**（S1-R1 讨论后明确，对应四阶段战略第 ① 阶段）：

1. **学会用 ERPNext**——执行者（用户）业务零基础、仅有软件工程知识，需通过实操跑通弹簧厂全链业务建立能力。
2. **摸清原生适用边界**——哪些环节原生够用、哪些靠培训与流程改造、哪些必须深度二开。

两者由同一份产物承载：L0-L10 分层学习路径，每层五段（该层要建什么 / 背后的数据模型 / 弹簧厂在这层的场景 / 该层痛点与原生缺口 / 判定）。学与摸底是同一份材料的两面。

**背景约束**：客户规模未知、需求未明确、未实地考察，且**项目内无任何弹簧资料**（场景集按行业通识生成，其数值只具行业典型值效力）。本 Phase 不产代码，只产判断依据与学习材料。

| Stage | 名 | 编排 | 目的 |
|---|---|---|---|
| S1 | 场景摸底 | 自由编排（R1 `Sha000` 已收敛 → R2 `Bui000` 已落地 → 待用户实操） | 设计覆盖弹簧厂全环节的场景集并判定原生适用度；产最小闭环操作稿与 L0-L10 教材 |

**本 Phase 收口标志**：① 最小闭环已由用户实操跑通；② L0-L10 十一层教材产出，每层的原生缺口有明确判定；③ 全部 ⚠ 项（当前 10 项）已按优先级排序——足以作为 devBlueprint 的输入。

## 架构变更（增量）

无。本 Phase 不动代码与架构。

## 延迟需求登记册

（列：编号 | 需求描述 | 来源 | 登记日期 | 唤醒条件 | 原始记录 | 状态）

暂无。S1 内提出的条目先记在 [S01-场景摸底/P1-S1-概况.md](S01-场景摸底/P1-S1-概况.md) 的本 Stage 登记册（SH-P1S1001 ~ 003），S1 收口时逐条判做了没有，未做的换发 `PH-` 号推进本表。
