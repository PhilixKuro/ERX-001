# P1 需求摸底 — Phase 概况

> Phase 级概况：级联指针（→ 当前 Stage）+ 本 Phase 开发范围 + 架构变更（增量）+ 延迟需求登记册。

## 级联指针

- **当前 Stage**：**S2 演示蓝图（进行中）** → [S02-演示蓝图/P1-S2-概况.md](S02-演示蓝图/P1-S2-概况.md)
  - 走 **`devBlueprint` Workflow**（**Phase 级**规划，分解出本 Phase 内的后续 Stage）
  - **进行到 B 步已完成**（R3，2026-09-24，产出 [0-P1文档/需求图谱.md](0-P1文档/需求图谱.md)）→ **下一步 C 步（`shape`，架构讨论）**。**Stage 内部进度以 Stage 概况「当前进度」为准**，本指针只到 Stage 一级
- **S1 场景摸底已收口**（2026-09-22，末轮 R6 `Pai000`）→ [S01-场景摸底/P1-S1-概况.md](S01-场景摸底/P1-S1-概况.md)
- S1 绑的是一次自由编排；**S2 绑的是 `devBlueprint` Workflow**

## 本 Phase 开发范围

**双重目的**（S1-R1 讨论后明确，对应四阶段战略第 ① 阶段）：

1. **学会用 ERPNext**——执行者（用户）业务零基础、仅有软件工程知识，需通过实操跑通弹簧厂全链业务建立能力。
2. **摸清原生适用边界**——哪些环节原生够用、哪些靠培训与流程改造、哪些必须深度二开。

两者由同一份产物承载：L0-L10 分层学习路径，每层五段（该层要建什么 / 背后的数据模型 / 弹簧厂在这层的场景 / 该层痛点与原生缺口 / 判定）。学与摸底是同一份材料的两面。

**背景约束**：客户规模未知、需求未明确、未实地考察，且**项目内无任何弹簧资料**（场景集按行业通识生成，其数值只具行业典型值效力）。本 Phase 不产代码，只产判断依据与学习材料。

| Stage | 名 | 编排 | 目的 | 状态 |
|---|---|---|---|---|
| S1 | 场景摸底 | 自由编排（R1 `Sha000` → R2 `Bui000` → R3 `Pai000` → R4 `Bui000` → R5 `Bui000` → **R6 `Pai000`**） | 设计覆盖弹簧厂全环节的场景集并判定原生适用度；产最小闭环操作稿与 L0-L10 教材 | **已收口** |
| **S2** | **演示蓝图** | **`devBlueprint`（Phase 级）** | **结合 S1 所得，做针对客户演示的需求讨论**——定需求图谱、定架构、分解出本 Phase 内的后续 Stage | **进行中** |

**⚠ 本 Phase 的目标已被用户重定向**（2026-09-22，S1-R6 第 10 步）。原收口标志的 ②③ 两项**未达成即收口**，这是裁决结果而非流程遗漏：

| # | 原标志 | 实际 |
|---|---|---|
| ① | 最小闭环已由用户实操跑通 | **✅ R3 达成** |
| ② | L0-L10 十一层教材产出 | **⚠ 只出 L0-L2 三层**。余八层**暂停**——用户裁决近期重整导航、远期用 React 全面重写 UI，在此前提下写操作路径会立刻失效 |
| ③ | 全部 ⚠ 项已按优先级排序 | **⚠ 未达成**。余 4 项未读源码（B4/E7/F5/H7），另有 R5 新增 5 项 + R6 新增 11 条译名缺陷待并入 |

**新的最优先目标（用户原话）**：**在客户面前跑通整个流程。** 为此三件事按序做——① **译名准确稳定**（"不应该出现有些翻译了有些没翻译的情况"）；② **导航重整**（按业务流重排入口）；③ **外部操控机制**（供演示脚本 / MCP / React 前端共用）。**远期全面重写 UI，技术栈 React。**

**故本 Phase 的收口标志改为**：S2 的 `devBlueprint` 产出需求图谱 + 架构 + 路线，把"客户演示要什么"分解为可开工的 Stage 序列。**②③ 两项转为 S2 需求讨论的输入**——演示需求本身就要重排一次优先级，按新口径处理比在 S1 内按旧口径补完更合理。

**收口标志第 ③ 项的进度**（R5 推进一项）：LG-021 证明 R1 的 ⚠ 判定因"grep 未找到即推定缺口"而偏严，该风险**已应验三次且全部是"范围缩小"方向**——D2（R3，`fixed_time` 原生存在）、中国科目表（R5，`unverified/` 下有一份 82 节点的 2006 准则版）、B1（R5，BOM 版本管理原生完整故 ⚠ 拆半）。

**故余下 4 项未读源码的 ⚠**（B4 模具工装 / E7 SPC・CPK / F5 寄售 VMI / H7 银行对账）**预计仍有 1-2 项会缩小或改判**。~~应在各自所属层产出时逐项复查~~ → **该安排已失效**（L3-L10 暂停），改由 S2 的需求讨论按新口径处理。

**R6 另证明这条教训有镜像形态**（S1 概况 LG-067）：LG-021 是"查不到就推定没有"，R6 三次判错导航是"**查到一张名字像的表就推定它是那个东西**"。两者同源——**拿间接证据当直接证据**。故此后凡断言界面呈现，须数据层与渲染层两层都有依据。

**⚠ 清单本身也在增长**：R5 新增 5 项待排序（展开长度公式无处存放、变体属性值非数值、~~`Routing` 无基准量~~、~~`setup_time` 缺失~~、图纸文件版本与通知）。

**⚠ 其中两项已被 P1-S2-R2 复核推翻或降级**（LG-021 第四次应验，仍是范围缩小方向）：

| 原记载 | 复核结论 |
|---|---|
| **`Routing` 无基准量** | **不成立。** `BOM Operation.batch_size`（Float，default 1，`fetch_from: operation.batch_size`）**就是 MRP II 意义上的工时基准量**，且在工时路径被真实消费——`work_order.py:1490-1497`：`if not d.fixed_time:` 时若 Operation 勾了 `create_job_card_based_on_batch_size` 则 `qty = d.batch_size`，随后 `time_in_mins` 除以（exploded 时乘以）它。单测佐证 `time_in_mins=40, batch_size=5` → WO qty=1 得 8.0、qty=5 得 40.0。**"每 100 件耗时 X"原生可表达。**<br>**真缺口小得多且换了性质**：工时基准量与任务单拆批粒度**共用同一个开关**，要"每 100 支 X 分"就被迫每 100 支开一张 Job Card（`work_order.py:2902-2910`）。**是耦合缺陷，不是能力缺失。**<br>**当初为何判错**（三因叠加）：① 上游 `Operation.batch_size` 带 `depends_on`，不勾开关界面不显示；② `BOM Operation` 侧该字段落在**成本节**、label 只叫 "Batch Size"、**无 description 无 `in_list_view`**，从工时视角看不出它是工时基准；③ 工时路径被那个"讲任务单"的开关门控，不勾即退回 `BOM.quantity`。**故"全厂共用同一 BOM 基准量"这个观察对默认路径是对的——错在当成了唯一路径。** |
| **`setup_time` 缺失**（据称故表达不了"20 分换型 + 每支 0.05 分"） | **部分成立，应降为"便利性改造"而非缺口。** 成立的半句：**单行内**固定与线性二选一、不相加（`work_order.py:1548-1551 calculate_time()` 仅一句 `if not d.fixed_time: d.time_in_mins = flt(d.time_in_mins) * flt(self.qty)`），且 `setup_time`/`setup_mins` 全仓**零命中**。<br>**不成立的半句**：Routing **拆成两行**即可原生表达——一行 `fixed_time=1, time_in_mins=20`，一行 `fixed_time=0, time_in_mins=0.05`，用同一 `sequence_id` 并行（该字段 description 明写此用法）。**故 `setup_time` Custom Field 与改 `calculate_time()` 均非必需。** |

**故 LG-055「两者合起来构成工时模型标准化、是 C3/C4 的共同前置」这一判断的前提已变**——工时模型原生基本够用，真正的前置是**换型矩阵**（工序对工序，非单值 `setup_time`）。

**另一项已核实为部分不准**：`Item Variant Attribute.attribute_value` **确是 `Data`（字符串）**故值不可直接参与计算、范围查询是字符串比较（LG-054 原话成立）；**但属性定义层原生有数值能力**——`Item Attribute` 有 `numeric_values`(Check)/`from_range`/`to_range`/`increment`，`item_attribute.py:86-105` 在 `numeric_values` 为真时校验范围与步进。**故"线径 0.5~5.0 步进 0.1"这类约束不必自建**，差的只是"属性代入公式算用料量"。

前两项（展开长度、变体属性）**不在 R1 的 52 场景里**（场景集按业务环节切，这两条是数据粒度的）。排序时须把"前置型"与"并列型"分开看，详见 [S1 概况](S01-场景摸底/P1-S1-概况.md) 的 LG-054・LG-055。

## 架构变更（增量）

无。本 Phase 不动代码与架构。

## 延迟需求登记册

（列：编号 | 需求描述 | 来源 | 登记日期 | 唤醒条件 | 原始记录 | 状态）

**S1 收口时逐条判定：9 条全部未做**（本 Stage 只摸底不实现），按规范 §12.2 整批上交本表，换发 `PH-` 号。**描述不在本表重复**——详情回 [S01-场景摸底/P1-S1-概况.md](S01-场景摸底/P1-S1-概况.md) 的本 Stage 登记册查（规范 §12.1：同一条的描述只在提出它的那一级）。

**⚠ 优先级已被 R6 重排**：原先九条大多挂在"第 ② 阶段（通用版）开工时"，现在**前三条成了客户演示的前置**，唤醒条件因此改变。

| 编号 | 一句话 | 唤醒条件（R6 重排后） | 原始记录 | 状态 |
|---|---|---|---|---|
| **PH-P1001**（原 SH-P1S1004） | **中文译名改造** | **最优先，客户演示前必须做完。** 硬前置是"目标侧术语标准"（须用户参与），依据盘点见 S1 概况 LG-070 | P1-S1-R3 协作记录第 5 步 + P1-S1-R6 协作记录第 10 步 | 延期 |
| **PH-P1002**（原 SH-P1S1006） | **前端不刷新即不显示**（五例，含 R6 改判的树视图空白） | **客户演示前**——不能让客户手动刷新。五例可能同源，集中查一次 | P1-S1-R3 协作记录第 16 步 + P1-S1-R6 协作记录第 6 步 | 延期 |
| **PH-P1003**（原 SH-P1S1009） | **外部操控编排机制**（演示脚本 / MCP / React 共用） | **客户演示前**，排在译名与导航重整之后。2-4 周，**长期资产** | P1-S1-R6 协作记录第 10 步 | 延期 |
| PH-P1004（原 SH-P1S1001） | 中国本地化（专票 / 金税 / 银行对账 / 财报格式） | 第 ② 阶段开工时；**给客户演示前必须有方案**。R5 查明有 unverified 中国科目表可作素材 | P1-S1-R1 第 1 步 | 延期 |
| PH-P1005（原 SH-P1S1005） | 工时模型与规格参数标准化（六项递进） | ④ 建议提前（成本极低、消除持续性错误）；②③ 是 C3/C4 的共同前置；全量排查须先做 | P1-S1-R3 协作记录第 16、15 步 + P1-S1-R5 | 延期 |
| PH-P1006（原 SH-P1S1003） | 客户可读版演示材料 | **R6 使其时点提前**——客户演示已成最优先目标。产出形态待用户定 | P1-S1-R1 第 2 步 + R1 收尾裁决 | 延期 |
| PH-P1007（原 SH-P1S1007） | Link 字段与报表支持拼音检索 | 第 ② 阶段，与译名改造同批 | P1-S1-R3 协作记录第 21 步 | 延期 |
| PH-P1008（原 SH-P1S1008） | 报表异步/实时更新可控化 | 数据量大到必须异步时。简单处置：把「库存余额」的 `prepared_report` 改 0（零代码） | P1-S1-R3 协作记录第 23 步 | 延期 |
| PH-P1009（原 SH-P1S1002） | 计件工资能力（hrms 未装） | 确认客户是否行计件工资制后 | P1-S1-R1 第 1 步 | 延期 |
