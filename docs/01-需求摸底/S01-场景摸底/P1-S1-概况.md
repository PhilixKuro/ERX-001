# P1-S1 场景摸底 — Stage 概况

> Stage 级概况：当前编排是哪条、进行到哪个轮次、本 Stage 延迟需求登记册。

## 当前编排

**一次自由编排**（非 Workflow）。理由：本 Stage 目标是"摸清原生 ERPNext 对弹簧厂的适用边界"，用哪几个 play、什么顺序要边做边定——讨论清场景后可能需要 spike 验证某个原生能力到底行不行，也可能直接收口转 devBlueprint。无一条 Workflow 的固定步骤序列与之吻合（devBlueprint 是下一步的事，本 Stage 是它的输入）。

## 当前进度

| Round | Free Spec | 工作项 | 状态值 |
|---|---|---|---|
| R1 | `Sha000` | 场景讨论——设计弹簧厂全环节场景集并逐场景判定原生适用度 | **已收敛** |
| R2 | `Bui000` | 最小闭环——产「最小闭环手把手操作稿」（23 环节，含三个 ★ 节点） | **已落地** |
| R3 | `Pai000` | 闭环实操——用户实操跑通 23 环节，Claude 在环陪跑取证、暴露偏差 | **已解决** |
| R4 | `Bui000` | 操作稿回改——按 R3 暴露的偏差整批改写「最小闭环操作稿」（v1→v2） | **已落地** |
| R5 | `Bui000` | L0-L2 教材——按 DEC-012 产 L0-L10 首批三层，并顺手复查 B1 源码 | **已落地** |
| R6 | `Pai000` | L0 实操——备份后重装造干净起点，带用户走 L0 段①，教材偏差就地改写 | **需另起一件事** |

## 本 Stage 已收口（2026-09-22）

**末个轮次**：R6（`Pai000`，L0 实操）。
**下一步**：**起 S2，走 `devBlueprint` Workflow**——结合本 Stage 内容，做**针对客户演示的需求讨论**（用户第 10 步裁决）。

**为何在 L0 未走完时收口**：R6 开工时定的"一件事"是走完 L0 十一步，实际只走完第一至三步。**但用户在 R6 第 10 步整体转向**——裁决了新的工作序列（译名优先 → 导航重整 → 外部操控机制；远期用 React 全面重写 UI），并要求收口本 Stage 转 `devBlueprint`。**L0 剩余八步的前置已变**：导航将重整、译名将改造，此刻续写操作路径会立刻失效。故 S1 在此收口，L0-L10 的续写待新 UI 定稿后重启。

**本 Stage 收口标志的完成情况**（对照 Phase 概况的三项）：

| # | 标志 | 状态 |
|---|---|---|
| ① | 最小闭环已由用户实操跑通 | **✅ R3 达成**（全链跑通、账实相符、成本链可追至源头输入） |
| ② | L0-L10 十一层教材产出 | **⚠ 部分达成——只出 L0-L2 三层**（33 项逐项判定）。余八层因 UI 将重写而暂停，非放弃 |
| ③ | 全部 ⚠ 项已按优先级排序 | **⚠ 未达成**。R5 复查掉 B1，**余 4 项未读源码**（B4 模具工装 / E7 SPC・CPK / F5 寄售 VMI / H7 银行对账）；另有 R5 新增 5 项 + R6 新增 11 条译名缺陷待并入排序 |

**②③ 两项未达成即收口，是用户裁决的结果**，非流程遗漏。它们转为 S2 `devBlueprint` 的输入——**客户演示的需求讨论本身就要重新排一次优先级**，届时按新口径处理比在 S1 内按旧口径补完更合理。

### R6 的进度详情

- **R6 产物**：[R06-L0实操/P1-S1-R6-Pai000协作记录.md](R06-L0实操/P1-S1-R6-Pai000协作记录.md)（10 步）+ [R06-L0实操/P1-S1-R6-Pai000回执.md](R06-L0实操/P1-S1-R6-Pai000回执.md)
- **跨轮演进产物改动**：[L0-记账底座与组织骨架.md](0-P1-S1文档/L0-记账底座与组织骨架.md) **v1 → v2.3**（导航方式改点击路径、新增第零步、第一/四步重写、段②新增导航数据模型一节、段④新增 5 条译名缺陷）
- **R6 的核心结论**（四条）：
  - **站点已重装**，现为"L0 走到第三步"的中间态：`华东弹簧` 由用户手工建出，95 科目 / 5 仓库 / 2 成本中心，**财年 0 条、默认公司为空、无任何业务数据**。R3 那套闭环样本可从 `20260922_145623` 恢复。
  - **教材第一至三步的期望值零差异**（八个默认科目全中），证明那批值是 Standard 模板 + zh 译名的确定产物、非 R3 偶然。
  - **三处改判既有判断**（详见下方长效信息 LG-066~068）。
  - **用户裁决了 UI 方向**（详见下方 DEC-020~022），这是 S1 收口转 S2 的直接原因。
- **R5 产物**：[R05-L0L2教材/P1-S1-R5-Bui000回执.md](R05-L0L2教材/P1-S1-R5-Bui000回执.md) + 三篇跨轮演进产物：[L0-记账底座与组织骨架.md](0-P1-S1文档/L0-记账底座与组织骨架.md)（501 行）、[L1-物料主数据与规格体系.md](0-P1-S1文档/L1-物料主数据与规格体系.md)（527 行）、[L2-工程数据BOM与工艺.md](0-P1-S1文档/L2-工程数据BOM与工艺.md)（643 行）
- **R5 的核心结论**（四条）：
  - **按用户三条裁决落地**：操作路径**自成一体**（不用"指针+补差"，三篇段①各自完整重写操作步骤）；**B1 归 L2 且本轮复查源码**；体量取"写透"档（实际略超，理由见回执复核建议第 4 条）。
  - **本轮新做 17 项源码取证**，其中最重要一项是**中国科目表模板存在**（见下方 LG-052）。
  - **对 R1 的 ⚠ 判定作三处更正，全部是"范围缩小"方向**——LG-021 那条教训在本批应验三次。详见下方长效信息节。
  - **新发现 2 个 ⚠ 且不在 R1 的 52 场景里**（展开长度公式无处存放、变体属性值是字符串），两者共用同一个二开动作、**是本项目目前性价比最高的二开点**。已并入 `SH-P1S1005`。
- **R5 留的三条待查**（均已在教材内标明实测办法，留给后续轮次顺手做）：① `ref_code` 是否有跨 Item 查重校验；② 变体属性按 `attribute_value` 做范围筛选的实际行为；③ 把「存货」科目转 group 的实际操作顺序。

### R4 的历史进度（R5 之前）
- **R4 产物**：[R04-操作稿回改/P1-S1-R4-Bui000回执.md](R04-操作稿回改/P1-S1-R4-Bui000回执.md) + 改写后的 [0-P1-S1文档/最小闭环操作稿.md](0-P1-S1文档/最小闭环操作稿.md)
- **R4 的核心结论**：六类改动落地（译名 31 处、三条实质性错误、四处漏写、8 条上游缺陷就地提示、数值改双列口径、以及三处补充）。**两条待查项就地实测闭合，其中 LG-050 的原推断被证伪**——ERPNext 不自动处理采购价差，靠"不许不一致"回避（两道默认拦截）；关掉拦截后差额残留在暂估库存科目、无人冲。**LG-051 查清成因后判为非 BUG**（repost 后台任务未执行，与 LG-011 同一前提条件）。
- **R4 定下的两条**：
  - **L0-L10 各层独立成篇**，不续写进操作稿（用户裁决，R2 回执「拿不准处」第 2 条由此闭合）。落点仍是 `0-P1-S1文档/`。
  - 操作稿**至此定稿**，后续只在发现新偏差时就地修正，不再扩容。
- ~~**下一步**：按 DEC-012 起 L0-L10 教材首批（L0-L2）~~ → **R5 已做完。**
- ~~**下一步**：按 DEC-012 起第二批 L3-L4~~ → **已作废（R6 第 10 步用户转向）**。L3-L10 八层**暂停**，待新 UI 定稿后重启——在 UI 将重写的前提下写操作路径会立刻失效。L0-L2 三篇的**段②④⑤ 继续有效**（不依赖 UI），**段①（操作路径）待导航重整后重写**。
- **⚠ 项复查仍欠 4 项，已随 S1 收口移交 S2**：Phase 收口标志第 ③ 项要求"全部 ⚠ 项已按优先级排序"，R1 那 10 个 ⚠ 原有 7 项未读源码。**R5 复查掉 B1（改判一半），故余下 4 项**：B4（模具工装，属 L2/L7）、E7（SPC/CPK，属 L6）、F5（寄售 VMI，属 L3/L4）、H7（银行对账，属 L8）。C3 已由 L2 缺口四部分解释（缺 `setup_time` 是其成因）、D6 已由"hrms 未装"确认、H5 已由 L0 段④缺口四确认。**按 LG-021 三次应验的比例，预计余下 4 项仍有 1-2 项会缩小或改判**。**原计划"在各自所属层产出时逐项复查"因 L3-L10 暂停而失效**，改由 S2 的需求讨论按新口径处理。
- ~~**本 Stage 收口后**：转 `devBlueprint`（用户预告）~~ → **已执行，见本文件「本 Stage 已收口」节。**

### R3 的历史进度（R4 之前）
- **R3 产物**：[R03-闭环实操/P1-S1-R3-Pai000协作记录.md](R03-闭环实操/P1-S1-R3-Pai000协作记录.md)（35 步）+ [R03-闭环实操/P1-S1-R3-Pai000回执.md](R03-闭环实操/P1-S1-R3-Pai000回执.md)
- **R3 的核心结论**：闭环全链跑通，账实相符（库存估值 424.50 = `存货 - HDS` 科目净额，「库存与账务价值对比」报表 0 行差额），成本链可从毛利一路追至"钢价 7.5 元/kg"与"机时费率 50 元/小时"两个源头输入。**操作稿暴露 20 余条偏差，含 3 条实质性错误（操作路径走不通），全部已登记、回改留给下一 Round。**
- **R3 的记录验证已补做**（`Fid001` 作**附属步骤**，`附属=新Session做`、**不占 Round**，规范 §4.6）：产 [R03-闭环实操/P1-S1-R3-Pai000忠实度报告.md](R03-闭环实操/P1-S1-R3-Pai000忠实度报告.md)，状态值 `有发现`。45 条有效发现已按用户裁决**全部修完**（2 条经用户指出后撤回）。要点三条：
  - **取证质量远高于记录质量**——B 类 239 条声称实跑 235 条、**无一条编造**；源码行号 14/14 准、译名 29/29 准、GL/SLE/单据 35/35 准；三条核心结论（账实相符、`process_loss` 零命中、`fixed_time` 存在）独立复核成立。**故 R3 的实操验证结论可信。**
  - **结构性违规**：63 个真实用户轮压成 35 步（26 步系事后补记，占 74%），三段补记声明"按一轮一步、不合并"与产物相反。**这暴露讨论契约 §7 的机械判据在补记场景下失效**——已在记录头部如实标注（不可逆）。
  - **三条判断问题已分别处置**：LG-037 归因错已闭合（真因 `manufacturing_settings.py:51` 的 `or 10` 兜底，且该字段填 0 无法取消工序间隔）；第 15 步"费率回写 BOM"作用域错判已更正（实际只回写 Routing）；第 24 步价差分录未查证已登记 LG-050 待实测。
- ~~**R3 仍未做的一件事**：未回改操作稿~~ → **R4 已做完**，含忠实度审核补出的全部项（FD-05/07/08/35/41）与 LG-046~049 四条漏答（已在环节 19、13、2、4 就地补入）。

### R3 站点终态（可作 L0-L10 教材的活样本）

| 类别 | 内容 |
|---|---|
| 公司 | `华东弹簧`（HDS），95 科目 / 6 仓库 / 2 成本中心，永续盘存开启、FIFO |
| 主数据 | 线材 `WIRE-65MN-2.0`、成品 `SPR-C-2.0-16-50`（含 Kg 换算 0.00868）、UOM `支`、三工站（各 50 元/小时）、三工序、Routing `压缩弹簧-标准`、BOM（1000 支基准、总成本 151.767）、客户 `江南机械`、供应商 `宝钢弹簧钢丝`、员工 `张师傅` |
| 单据链 | 采购侧 PO→PR→PI→PE（750 元）；销售侧 QTN→SO→PP→WO→领料→3×JC→完工入库→DN→SI→PE（1045 元） |
| 备份基准点 | `20260919_225632`（清场后的干净空站点）、`20260921_042056`（闭环已跑通） |

### R2 之前的进度（历史）

- R2 已落地时的下一步是"由用户实操跑通最小闭环"，该项由 R3 承接完成。
- **R2 产物**：[0-P1-S1文档/最小闭环操作稿.md](0-P1-S1文档/最小闭环操作稿.md)（跨轮演进产物）+ [R02-最小闭环/P1-S1-R2-Bui000回执.md](R02-最小闭环/P1-S1-R2-Bui000回执.md) + [R02-最小闭环/P1-S1-R2-Bui000讨论记录.md](R02-最小闭环/P1-S1-R2-Bui000讨论记录.md)（中途讨论，产 DEC-013）
- **下一步：由用户实操跑通最小闭环**。这不是一个 Round——它是用户自己的动作，产物的验证靠它完成。实操中发现操作路径与实际界面不符时，**就地改写操作稿**（它是跨轮演进产物，改动须在当轮回执留痕 + 更新其版本变更记录表）。回执「复核建议」第 1 点列出了三处最可能卡住的环节与一处科目名待回填。
- **实操之后**：按 DEC-012 逐批产 L0-L10 教材（L0-L2 / L3-L4 / L5 最大 / L6-L8 / L9-L10 各一批，每批一个 Round，均跑 `Bui000`）。**L0-L10 的展开以"闭环已跑通"为前置**（DEC-009）。开工首轮须先定一件事：L0-L10 是续写进操作稿这同一份文件，还是各自独立成篇（见 R2 回执「拿不准处」第 2 条）。
- **本 Stage 收口后**：转 `devBlueprint`（用户预告），那将是新起一个 Stage。

## 本 Stage 的长效信息（寿命与本 Stage 相同）

**R1 定下的、后续各 Round 须遵守的约束**（编号对应 R1 讨论记录的决策汇总表）：

| # | 约束 |
|---|---|
| DEC-004 | 弹簧类型主线走**冷卷圆簧**；B 类（异形线成型）/ C 类（板簧碟簧冲压）的差异只在 L1 物料规格与 L2 工程数据两层加差异节，B4 模具管理写透 |
| DEC-005 | 规模档位以 **B 小型（50-150 人，多机台多班）** 为主线；判定会随规模翻转的场景（主要 D 域与 I 域）标注 A/C 档差异 |
| DEC-006 | 本 Stage 只产**内部稿**；客户可读版另起 Round 提炼 |
| DEC-008 | 产物形态为 **L0-L10 分层学习路径**，每层五段（要建什么/数据模型/弹簧厂场景/痛点缺口/判定）；52 场景全图保留作索引视图 |
| DEC-010 | 教材深度＝**操作路径 + 背后数据模型**两层，不含会计原理层 |
| DEC-011 | 一律在**新建的干净弹簧厂环境**里做，不用 ERX Demo 演示数据 |

**R2 定下的约束**（编号对应 R2 中途讨论记录的决策汇总表）：

| # | 约束 |
|---|---|
| DEC-013 | 干净弹簧厂环境＝**在现站点 `erx.localhost` 内新建公司**（公司名 `华东弹簧`、缩写 `HDS`），动手前先清 demo 数据与 `ERX Demo` 主公司。**不新建站点**——`up.sh` 只建一个 `SITE_NAME` 站点，第二站点须自己补 v16 那五处初始化且换机器会丢。后续各 Round 一律在这个公司下操作 |

**LG-005 已关闭**（R1 遗留的"新建站点 or 新建公司"由 DEC-013 裁决）。

**R2 留给用户实操验证的两条**（非延迟需求，故不入下方登记册）：

| # | 待验 |
|---|---|
| LG-011 | ~~`seed-demo.sh --clear` 的删除走后台队列，须 `docker/start.sh` 在跑才真正执行~~ → **R3 已验证为真且前提满足**：honcho 在跑故一次清到位，`Transaction Deletion Record` 查询返回 0 条（任务已执行完并自行清理）。操作稿写入的判据成立 |
| LG-012 | ~~`ERX Demo` 主公司能否直接删未实测~~ → **R3 实测可删**：未加 `force`、无 `LinkExistsError`，级联清掉 95 科目 + 5 仓库 + 2 成本中心。**操作稿给的退路（改名 `ZZ-废弃-` 并停用）用不上**，回改时标注"已实测可删" |
| **LG-013** | ~~用户报「搜 `Fiscal Year` 英文原名也无结果」，故"搜不到就搜英文"这条通用办法的可靠性待查~~ → **R4 已闭合，该办法普遍可行**。① 英文回退机制确实存在：`search_utils.js:595-602` 的 `fuzzy_search` 先按译名匹配、**得分为 0 且界面非英文时自动回退用英文原名再匹配一次**（已在容器内构建产物 `desk.bundle.*.js` 里验证该逻辑已编译进前端）；② **`Fiscal Year` 那次的真因是 `awesome_bar.js:176-179`**——输入含空格时，空格后的词被当作**类型过滤词**（只认 `List`/`Tree`/`Report`/`New`），`Year` 不匹配任何类型故那一路返回空。**办法：搜不带空格的片段**（搜 `Fiscal` 即出）。已写进操作稿开头 |
| **LG-050** | ~~第 24 步②的「价差」分录未经查证（R3 整轮无查询，凭机制推断而以确定性口径记入，忠实度报告 FD-41 判为判断问题）~~ → **R4 实测闭合，原推断被证伪**。① **默认设置下该情形根本发生不了**：`Buying Settings.maintain_same_rate=1` 且 action 为 `Stop`、`Accounts Settings.over_billing_allowance=0`，两道独立拦截且 `role_to_override_stop_action` 为空故无角色可绕过（校验在 `accounts_controller.py:953-980`，容差 0.01）；② **关掉两道拦截后分录里没有价差科目**：`借 暂估库存 78 / 贷 应付账款 78`，收货时只贷记 75，**差额 3 元残留在暂估科目、净额 +3、无人冲**，库存估值不动（SLE 0 条）。**故 ERPNext 不自动处理采购价差，靠"不许不一致"回避。** R3 记录里那三行（含"借价差 30"）是虚构形态。**Landed Cost Voucher 那条路与"中国厂该怎么做"仍未实测**，已在操作稿环节 10 标明、并列入「留给 L0-L10 的账」 |
| **LG-051** | ~~`Bin.stock_value` 与 SLE 存在 0.07 不一致（忠实度报告 FD-43，原归因"已取消的单据未在 Bin 回滚"）~~ → **R4 查清成因，判为非 BUG、直接闭合**。**原归因错**：与"取消"无关。真因是 `stock_ledger.py:604-610`——`if not future_sle_exists(...)` 时才 `update_bin()`，**存在更晚的库存流水时跳过即时更新**，改建 `Repost Item Valuation` 交后台重算（正确设计：插历史单据会改 FIFO 队列、其后每笔估值都要重算）。实测站点积压**三条 `Queued` 记录**（两条 R3 当天留下、一条 R4 的），**故 R3 那 0.07 与 R4 实测的 75.0 是同一成因、非两个现象**。任务走 cron 每 15 分钟一次（`hooks.py:436`），**要 `docker/start.sh` 在跑才触发——与 LG-011 同一个前提条件**。**影响**：报表读 SLE 不读 `Bin` 故无影响；`Bin.actual_qty` 始终正确；`bin.py:40-71` 的 `recalculate_values()` 会自愈；生产环境下残差为暂态（≤15 分钟）。**唯一困惑场景是绕过报表直查 `Bin`**。**处置：不改代码、不入登记册**（同 LG-011），落在操作稿环节 12 的贯穿性前提框 + 环节 23 的说明 |
| LG-014 | ~~环节 20 第三行分录（工序成本转入库存价值）的科目名未定，R2 回执列为「高」风险~~ → **R3 已闭合**：科目为 **`结转库存的费用 - HDS`**（`account_type = Expenses Included In Valuation`）。**不是** R2 猜测的 `销货成本`；公司的 **`expenses_added_to_stock_account`** 字段为 None，故该科目须**逐行手填**（本例 11 行），须设默认值避免每单手填 N 行、且避免填错致成本失真。**注**：该字段名曾误记为 `expenses_included_in_valuation`（v13/v14 旧名，v16 源码零命中）——见 R3 忠实度报告 FD-07 |

**R3 定下的约束**（编号对应 R3 协作记录的决策汇总表）：

| # | 约束 |
|---|---|
| DEC-016 | 将来多客户部署走**一客户一 Site、每 Site 一家 Company**，不用 Company 隔离不同客户——Item / Customer / Supplier 无 `company` 字段、跨公司共享，用 Company 隔离构成数据泄露 |
| DEC-017 | 翻译改造采用 **A+B 叠加**（A = `Translation` DocType 记录、最高优先、零侵入；B = 自有 app 的 `translations/zh.csv` 做批量基线），**先中国大陆通用版、再弹簧行业专用版**。方案 C（改 erpnext 自带 zh.csv）已否决 |
| DEC-018 | **不改「没有权限」那条报错提示**——真因是 User Permission 而非默认公司为空，该场景在"一客户一 Site、每 Site 一家公司"的形态下不出现 |
| DEC-019 | "批量设置字段"（`set_warehouse` 等）的改法取 **B 双向同步**（清空头部时同步清空明细行），而非只加提示——行为一致性本身即防错，且校验层已兜住后果 |

**R3 暴露的、须后续 Round 处理的核心事实**：

| # | 事实 |
|---|---|
| **LG-021** | **R1 与操作稿"原生无固定工时"的论断错了**——`BOM Operation.fixed_time` 存在（`work_order.py:1550`：`if not d.fixed_time: time_in_mins *= qty`）。**故 D2 的 ⚠ 须拆为两项**：「批处理工时不随批量变」**原生有**、「一炉混多工单合并作业」原生无。**这是 R1 复核建议预警的"grep 未找到即推定缺口、可能使二开范围虚高"的第一个应验实例——其余 7 项未读源码的 ⚠ 应同样复查** |
| **LG-039** | **生产计划不按工艺损耗反算投料量**（`grep "process_loss" production_plan.py` 无命中），故每张按 SO 生成的工单必然短交。本例 SO 要 5000 → 工单 5000 → 完工 4750 → 缺 250 支。**真实代价不是"少 250 支"而是流程成本翻倍**（每张订单都要补做一次：多一张工单、一次领料、一次报工、一次入库、一次发货） |
| **工序成本依赖真实报工** | 本轮工序成本只记 0.27 元而非 433.3 元（三张任务单各只计时几秒），致毛利率虚高 40 个点（68.8% vs 应为 28.8%）。**证明 ERPNext 的成本准确性完全建立在车间真实报工之上**——这正是 R1 判 D2 为 ⚠、D3 为 🔧 的实质，也说明 R1 样板二"车间报工页"二开的价值不是省事而是**让成本数字可信** |

**R6 定下的约束**（编号对应 R6 协作记录的决策汇总表）：

| # | 约束 |
|---|---|
| **DEC-020** | **教材的导航方式用"点击路径"而非"搜索"**——写"哪个工作区/图标 → 哪张卡片或分节 → 哪个标签"。理由：搜索绕过界面结构，学的人建立不起"ERPNext 怎么组织功能"的心智模型，而理解骨架正是教材目的之一 |
| **DEC-021** | **UI 改造分近远期**：**近期（客户演示前）走 A+B**——① 译名准确稳定（最优先，用户要求"不应该出现有些翻译了有些没翻译"）；② 导航重整（按业务流重排 `Desktop Icon` + `Workspace Sidebar`）；③ 外部操控机制。**远期全面重写 UI，技术栈选 React**。近期在 jQuery 的 desk 上通过官方扩展点叠加，**与远期 React 无技术债传递**（desk 是 jQuery 不是 Vue，自建前端走 REST API v2，两套独立） |
| **DEC-022** | **"一路下一步"要的是外部操控机制，不是加按钮**——建一个编排层，供**演示脚本、MCP、React 前端三者共用**，演示只是它的第一个用例。流程定义**存成 DocType 数据不写死在代码里**（MCP 将来读同一张表即知有哪些可编排流程，即能力自描述）。四块官方接口已查实：REST API v2（含 `get_meta`）/ `run_doc_method`（官方明说给"不重实现业务逻辑的自建客户端"用）/ `execute_doc_method` / `publish_realtime`。**"界面自己动"的甲乙两方案未裁决**（甲=服务端推事件、Claude 推荐；乙=浏览器侧驱动） |

**R6 暴露的核心事实**：

| # | 事实 |
|---|---|
| **LG-066** | **v16 的导航由 `Desktop Icon`(36) + `Workspace Sidebar`(32) + `Workspace Sidebar Item` 三张表承载，`Workspace`(19) 不承载导航层级**——后者 `parent_page` 全空、装的是页面内容（快捷方式/卡片/图表），但它**是 `Desktop Icon` 的生成源**（`create_desktop_icons_from_workspace()`），故"源与产物"关系而非"废弃与现行"。**另两条机制要点**：① v16 路由前缀改为 `/desk/`，`/app/*` 降为 301 重定向（`hooks.py:69-71`）；② `hidden` 是否向下传递**取决于 `icon_type`**——`Folder` 传递、`App` 不传递（`desktop_icon.py:170-210` 的过滤只查父图标权限不查其 hidden），故 `库存`/`采购`/`销售`/`生产` 挂在 `hidden=1` 的 `ERPNext` 组下却仍显示在首页 |
| **LG-067** | **查界面呈现必须"数据层 + 渲染层"两层都查，且数量断言须标明是哪一层的数。** R6 连续三次判错导航（工作区平铺侧栏、首页顶层 7 个图标、首页图标与表单选项卡混淆），**三次共同错法都是"查到一张表就直接推界面"**。本例三个数字没一个相同：`Desktop Icon` 36 条 / 顶层 9 条 / 实际显示更多。**渲染层要查的是**：`boot.py` 的 `get_sidebar_items()`、`desktop_icon.py:170-210` 的过滤、`list_view.js:533-544` 的视图与空状态选择。<br>**R6 的记录验证使本条范围扩大**：它查出的 6 处问题里 4 处与这三次判错同源（`cat x 2>/dev/null` 无输出被读成"空文件"而该文件实不存在、引用一个无明细的计数当硬证据、把 `add_custom_button` 归到没有它的文件）。**通用形式是"判据必须能区分它要区分的两种情形"**——问一句"这个输出还有别的解释吗"，有就换命令。已升为长效约定写入 [开发守则](../../开发守则.md) |
| **LG-068** | **三处改判既有判断**：① 树形 DocType 列表页空白**不是"偶发、已兜底、不入登记册"而是必然的交互缺陷**（`account_list.js` 是空文件、实现在 `account_tree.js`，从侧栏点进恒落列表路由），改判入 `SH-P1S1006` 第五例；② `应收账款 1` 的 `1` 后缀成因**不是平级节点撞名而是父子译名撞车**（`Accounts Receivable` 与 `Debtors` 均译「应收账款」），**是翻译缺陷非模板怪癖**，且实操上"选科目要选带 `1` 的那个"（不带的是 group 不能记账）；③ **`20260919_225632` 备份不是"清场后的干净空站点"**而是清场前的 demo 状态（2 公司/10 Item/191 科目，`华东弹簧` 零出现）——**项目内本无干净空站点基准点**，R6 重装后才造出一个 |
| **LG-069** | **译名缺陷本轮新增 11 条，其中 4 条会导致操作出错**（非仅显示不美观）：① **`Setup` 与 `Settings` 都译「设置」**致同一侧栏两个分节同名（会点错地方）；② 父子科目译名撞车（会选错科目）；③ `Stock Expense` 节名与 `Expenses Added To Stock Account` 标签均未译（按中文找不到）；④ `默认成品仓(收料仓)` 括注错误（成品仓是出成品处，"收料"是原料仓的事）。另 7 条：`Opening & Closing` 误译「POS机交接班」、`Accounts Setup` 未译、`Sales Taxes` 未译、` Default Work In Progress Warehouse ` 未译且标签带首尾空格、`默认报废仓` 与「废料」不一致、`计量单位`/`单位` 同物两名、科目树两根用旧式术语且括号全半角混用（树上找不到"资产"）。**故 `SH-P1S1004` 的实际严重性比原记载高一档** |
| **LG-070** | **译名依据盘点结论：够立项、不够开工**（R6 第 10 步，子 Agent 通读操作稿+R3/R4 五个文件共 5247 行）。**已有资产**：8 张成表对照，其中三张核心表经 R3 忠实度报告独立重跑确认 **29/29 准**，可直接采信；技术路径全查清有源码行号（`translate.py:135-240` 五级优先级、`Translation` 记录最高优先、后装 app 覆盖先装、`context` 机制在本仓库是通的故多一条低侵入改法）。**但只有 5 条已知该改成什么**（全来自收付款表），**约 30 条只记了有问题没给目标**。**四个缺口**：① **用户举例的"销售与采购概念对调"零实例**——R3 只走一条闭环路径、从未把两侧同名字段并排比对过，故该关切现有文档既不能证实也不能定位，**必须重测**；② 模块覆盖偏斜（质检/外协/月结完全未涉及，质量/委外/资产/HR/CRM/项目/变体/批次/税务模板零覆盖）；③ 撞名只知 1 条且判了"必改"却无新名（LG-065），全量排查未做；④ **目标侧术语标准缺失**，是硬前置且须用户参与 |

**R5 暴露的核心事实**（L0-L2 教材产出时的源码复查所得）：

| # | 事实 |
|---|---|
| **LG-052** | **中国科目表模板存在，修正"完全无中国科目表"的既有表述**。`accounts/doctype/account/chart_of_accounts/unverified/cn_l10n_chart_china.json` 存在，`China - 中国会计科目表（财会[2006]3号《企业会计准则》）`，82 个一级节点（原材料/生产成本/库存商品/委托加工物资/应交税费下 11 个税种等）。**三个原因使它不可直接用**：① 在 `unverified/` 而 `get_charts_for_country()` 默认只扫 `verified/`（`chart_of_accounts.py:147-149`），故下拉里选不到（`verified/` 下 78 个模板无一以 `cn` 开头）；② **82 节点全部平铺、`root_type` 全空**，导进来是 82 棵无 `root_type` 的孤树，靠 `ignore_mandatory`（`:58`）绕过校验、报表归类失效；③ **内容停在 2006 准则**——含营改增后已废的「应交营业税」「营业税金及附加」，「应交增值税」下无进项/销项二级明细。**故第 ② 阶段的工作是"补全一份现成清单"而非"从零设计"，工作量显著减少但仍须懂中国准则的会计参与。已回填 `docs/项目概况.md`。** 惟**这份模板的会计质量未经会计审阅**（R5 回执复核建议第 1 条），若实际不可用则工作量回到接近从零设计 |
| **LG-053** | **B1 的 ⚠ 须拆为两项**：「**BOM 版本管理**」**原生完整，改判 ✅**——`autoname` 生成 `BOM-{item}-001` 递增序号且算序号时排除 amended（`bom.py:183-215`）、`is_default` 单选 + cancel 时强制置 0（`:729-751`）、`is_active`、`amended_from`、`track_changes=1`、amend 时附件自动复制（`frappe/model/document.py:627-633`）。「**图纸文件版本与变更通知**」**维持 ⚠**——`File` 无版本号字段、`Version` 的 `FIELDTYPES_TO_IGNORE`（`version.py:12`）排除 `no_value_fields` 故附件变动不进 diff（只在 `Comment` 留一行、不与 BOM 版本关联）、无开箱即用的改版通知、无"车间只看最新版"强制、图号不是一等公民。**二开范围缩小约一半。** 惟**这个拆分本身可能被推翻**——业务上"客户改图纸→改 BOM→车间要知道"是一条链，拆开判可能漏掉中间环（R5 回执复核建议第 2 条） |
| **LG-054** | **新发现 2 个 ⚠，且不在 R1 的 52 场景里**（场景集按业务环节切，这两条是数据粒度的）：① **展开长度公式无处存放**——线径/外径/圈数在 ERPNext 里没有可计算的数值字段，每建一个新规格要人拿计算器按一遍公式，算错无人发现，而这个数直接决定 BOM 材料成本；② **`Item Variant Attribute.attribute_value` 是 `Data` 类型**（字符串），故不可参与计算、范围查询不可靠。**两者共用同一个二开动作**（给 Item 加一组规格 Custom Field + 保存时计算 hook），**半天工作量、消除一类持续性错误，是本项目目前性价比最高的二开点**。已并入 `SH-P1S1005`。**L10 排优先级时别因"不在 52 场景里"而漏掉** |
| **LG-055** | **L2 的两个 ⚠ 是"前置型"缺口，L10 排优先级时不能与并列项同等看待**：「`Routing` 无基准数量字段」+「`fixed_time` 只有两档、无 `setup_time`」合起来构成**工时模型标准化**，而它是 **C3（换型合并）与 C4（插单与优先级）两个最高优先级 ⚠ 的共同前置**——没有换型时间数据，排产优化算法无输入。两者已在 `SH-P1S1005` 的 ②③ 层 |
| **LG-056** | **DEC-004 的"其余八层几乎一致"在 L7 这一点上不成立**（R5 判断，依据为行业通识非查证）：C 类（板簧碟簧冲压）的**热处理普遍外协**，而 A 类只有电镀外协，故 **L7 的权重 C 类显著高于 A 类**。这不是结构差异（机制相同）而是权重差异。**R1 复核建议的中风险项第二行由此部分应验**，L7 产出时须设专节 |

**场景集基础数据**：52 场景 / 9 域（A5 B6 C6 D7 E7 F5 G3 H7 I6）/ ✅28 🔧13 ⚠10 ❓1。⚠ 十项为 B1 B4 C3 C4 D2 D6 E7 F5 H5 H7。

**⚠ 判定的复查进度**（Phase 收口标志第 ③ 项的前置）：

| 状态 | 项 |
|---|---|
| 已读源码 | C4、D2（R1）、**B1（R5，改判一半）**、H5（R5 确认缺口）、C3（R5 部分解释成因）、D6（"hrms 未装"已确认） |
| **仍未读源码（4 项）** | **B4**（模具工装，属 L2/L7）、**E7**（SPC/CPK，属 L6）、**F5**（寄售 VMI，属 L3/L4）、**H7**（银行对账，属 L8） |
| 本批新增待排序 | LG-054 的 2 项（展开长度公式、变体属性值非数值）+ LG-055 的 2 项（`Routing` 基准量、`setup_time`）+ 图纸文件版本（LG-053 的后半） |

**LG-021 的教训已应验三次**（R1 的"grep 未找到即推定缺口"使二开范围虚高）：D2（R3）、LG-052 中国科目表（R5）、LG-053 B1（R5）。**三次全部是"范围缩小"方向。** 故余下 4 项复查时应预设"可能原生已有、只是命名或位置不同"，而非预设缺口成立。

**"已有资料"的含义（R1 收尾后用户明确）**：指**互联网上的公开资料与 Claude 知识库内的行业知识**，用户手头**没有任何一手资料**（无客户工艺文件、报价单、生产报表、图纸）。故场景集按行业通识生成是**符合要求的做法，不是缺陷**——LG-009 原先的"待补一手资料"前提不成立。

但由此派生一条**恒定约束**，后续各 Round 须遵守：**凡写进产物的行业数值（工时、损耗率、换型时间、批量、人数、设备参数），都只具"公开资料典型值"效力，不具"本客户实际值"效力。** 产物中给出此类数值时须标明其为行业典型值、并尽可能给出来源或区间而非单点值；实地考察时逐项校准。这一条对 L0-L10 全部教材有效。

**须提醒后续 Round 的风险**：10 个 ⚠ 中仅 C4 与 D2 读过源码，其余 8 项（B1/B4/C3/D6/E7/F5/H5/H7）的缺口判定系"grep 未找到相关字段"推出，待逐项验证——若某项实有原生机制只是命名不同，会把"可配置解决"误判为"须二开"、使二开范围虚高（见 R1 复核建议风险表第二行）。

## 本 Stage 延迟需求登记册

（列：编号 | 需求描述 | 来源 | 登记日期 | 唤醒条件 | 原始记录 | 状态）

本 Round 的遗留问题（LG-001 ~ LG-010）均记在 R1 讨论记录的遗留问题节，其中属"决定不在本 Stage 实现"的延迟需求如下：

| 编号 | 需求描述 | 来源 | 登记日期 | 唤醒条件 | 原始记录 | 状态 |
|---|---|---|---|---|---|---|
| SH-P1S1001 | 中国本地化改造（增值税专票、金税盘对接、银行对账、中国财报格式）——v16 `regional/` 无 china | R1 讨论记录 LG-001 | 2026-09-19 | 四阶段战略第 ② 阶段"做中国大陆通用版"开工时；给客户演示前必须有方案 | P1-S1-R1 第 1 步「⚠ 讨论中发现」 | 延期（本 Stage 只摸底不实现） |
| SH-P1S1002 | 计件工资能力——hrms app 未装，D6 无支撑 | R1 讨论记录 LG-002 | 2026-09-19 | 确认客户是否行计件工资制后，评估装 hrms 或自建 | P1-S1-R1 第 1 步能力核实表 | 延期 |
| SH-P1S1003 | 客户可读版演示材料 | R1 讨论记录 LG-004 | 2026-09-19 | **已明确（R1 收尾后用户裁决）**：待用户跑通最小闭环、且学完 L0-L10 教材后，**由用户决定如何产出**。此前的两个读法（"下一个 Round" / "通用版改造之后"）均作废——时点早于通用版改造、晚于下一个 Round，且**产出形态本身也未定**，不预设为"从内部稿提炼" | P1-S1-R1 第 2 步议题③ + R1 收尾后的裁决 | 延期 |
| SH-P1S1004 | **中文翻译改造**：v16 的 zh 翻译多处缺失或不准（实测 15 处菜单名与操作稿不符，`Item` 甚至未翻译）。分两版做——先中国大陆通用版、再弹簧行业标准专用版。技术路径为 **A+B 叠加**：B = 自有 app 的 `translations/zh.csv` 做批量基线（后装 app 覆盖先装），A = `Translation` DocType 记录做个别覆盖（最高优先、零侵入）。**方案 C（改 erpnext 自带 zh.csv）已否决**（NV-016） | R3 协作记录第 5 步议题⑤（用户提出） | 2026-09-19 | 四阶段战略第 ② 阶段"做中国大陆通用版"开工时；行业版待第 ④ 阶段定制时叠加。**本轮不做的三条理由**：改哪些词取决于弹簧行业标准叫法而用户尚未摸清；属第 ② 阶段工作面应与中国财税改造同批；半途改会让操作稿译名对照表再失效一次 | P1-S1-R3 协作记录第 5 步（含翻译加载优先级的源码取证，`translate.py:135-240`） | 延期 |

| SH-P1S1005 | **工艺路线与工时模型的行业标准化改造**，三层递进：① 短期——回火这类批处理工序勾原生 `fixed_time`（零开发，配置即可）；② ~~中期——给 `BOM Operation` 加 `setup_time` 并改 `calculate_time()`~~ → **已被 P1-S2-R2 复核降级为「便利性改造」**：Routing **拆两行**（`fixed_time=1,time=20` + `fixed_time=0,time=0.05`，同 `sequence_id` 并行）原生即得「20 分换型 + 每支 0.05 分」。仅"单行内不可相加"成立。**真正的前置是换型矩阵**（工序对工序，非单值 `setup_time`）；③ ~~长期——给 `Routing` 加基准数量字段~~ → **已被 P1-S2-R2 复核推翻**：`BOM Operation.batch_size` 原生即工时基准量（`work_order.py:1490-1497` 真消费），**改写为「解耦工时基准量与任务单拆批粒度」**——两者现共用开关 `Operation.create_job_card_based_on_batch_size`，量级由"加字段"下调为"解耦"。详见 [P1 概况](../P1-概况.md) 的复核表。**并由此展开一次全量排查：ERPNext 哪些常用流程（Routing / BOM 优先）未跟上行业现行标准，逐项列出并定二开范围**。<br>**R5 并入三项**（L0-L2 教材产出时发现，编号沿用本条不另发号）：**④ 规格参数的存放与计算**（给 Item 加线径/外径/总圈数 Custom Field + 保存时算出 `conversion_factor` 与 `weight_per_unit` 的 hook）——同时解决 LG-054 的两条（展开长度公式每料号手算、变体属性值是字符串不可计算），**半天工作量、性价比最高、建议排在 ①②③ 之前做**；**⑤ 图纸/程序文件的版本与变更通知**（LG-053 的后半，同时覆盖 A 类图纸与 B 类成型程序，宜与 B4 模具工装合并考虑）；**⑥ `rm_cost_as_per` 取到 0 时无提示**（新站点建 BOM 必撞，成本字段只读故手填不了，交互层问题） | R3 协作记录**第 16 步**（用户提出，USER#28）；**④⑤⑥ 由 P1-S1-R5 并入** | 2026-09-20（④⑤⑥ 为 2026-09-21） | 第 ② 阶段（通用版）做 ①②；③ 与全量排查视工作量排入 ② 或 ④ 阶段。**全量排查须先做**——它决定 ②④ 两阶段的二开范围，是 devBlueprint 的输入。**④ 建议提前**（成本极低、消除持续性错误）；**②③ 是 C3/C4 两个最高优先级 ⚠ 的共同前置**（LG-055），排序时不可与并列项同等看待 | P1-S1-R3 协作记录**第 16 步**（需求提出）+ **第 15 步**（含 `fixed_time` / `calculate_time()` 源码取证与 MRP II 标准工时模型说明）。**溯源步号原误记为第 15 步**，见忠实度报告 FD-09。**④⑤⑥ 的依据**见 [L1 教材](0-P1-S1文档/L1-物料主数据与规格体系.md) 段④缺口二/三、[L2 教材](0-P1-S1文档/L2-工程数据BOM与工艺.md) 段①第五步与段④缺口六、[P1-S1-R5-Bui000回执](R05-L0L2教材/P1-S1-R5-Bui000回执.md) | 延期 |
| SH-P1S1006 | **⚠ R6 补入第五例并改判一条**：树形 DocType（`Account`/`Warehouse`/`Cost Center`）**从侧栏点进恒落列表路由而显示空白**——`account_list.js` 是空文件、实现在 `account_tree.js`。**R3 原判"偶发（首次访问）、`breadcrumbs.js:211` 已兜底、不入登记册"被推翻**：那套兜底只管"无视图记录时的默认值"，管不了侧栏链接生成的显式列表路由，故是必然而非偶发。**办法**：走 `/desk/{doctype}/view/tree`。<br>——以下为 R3 原文——<br>**前端不刷新即不显示改动**，本轮已遇四例：① 仓库列表页空白（敲树视图地址后正常）；② 采购工作区侧栏「物料与价格」卡片不显示（数据层三查正常、服务端缓存已清、无痕窗口同样异常）；③ BOM 保存后「提交」按钮不出现（须刷新，服务端已验 `is_submittable=1`、`validate` 通过、有 submit 权限、无 Workflow 拦截）；④ 用户报"很多操作做完后不能第一时间显示"。**要求二开时让这类改表操作在改动后自动刷新，不依赖用户手动刷新** | R3 协作记录**第 16 步**（用户提出，USER#28③；四例累积于第 7、11、12、14、15 步）。**原误记为第 15 步**，见忠实度报告 FD-09 | 2026-09-20 | **第 ③ 阶段客户演示前必须闭环**——不能让客户手动刷新，这直接损害软件可靠性观感。四例可能同源（前端资源加载 / 表单 dirty 状态管理），**集中查一次而非分开查**；待本轮实操走完立即排查 | P1-S1-R3 协作记录第 16 步（提出）+ 第 7、11、12、14、15 步（四例） | 延期 |

| **SH-P1S1009** | **外部操控 ERPNext 的编排机制**（DEC-022）：建一个编排层，供**演示脚本、MCP、React 前端三者共用**。职能是"执行流程中的一步"——调服务端造/改单据 + 把界面导航到该单据 + 填上该填的值。**流程定义存成自有 DocType 数据**（如 `Demo Flow` + 子表 `Demo Flow Step`），不写死在代码里，使 MCP 将来读同一张表即知有哪些可编排流程（能力自描述）。**四块官方接口已查实**：REST API v2（含 `get_meta`，故外部可运行时得知表单结构）/ `run_doc_method`（在内存 doc 上跑控制器方法，官方 docstring 明说给"不重实现业务逻辑的自建客户端"用）/ `execute_doc_method`（提交已存库单据）/ `publish_realtime`（服务端推事件到浏览器，可定向到 user）。**不需改 ERPNext 源码。** 甲乙两方案未裁决：甲=服务端推事件驱动（Claude 推荐，真正的外部操控、MCP 可复用、可拿手机当遥控器，但要装约百行前端监听）；乙=浏览器侧驱动（略简单但控制权仍在 desk 内，MCP 与 React 复用不到驱动界面那部分） | R6 协作记录第 9、10 步（用户提出） | 2026-09-22 | **客户演示前**。排在译名与导航重整之后（按钮与提示文案要用到译名，反序会改两遍）。预估 2-4 周，但**它是长期资产非演示一次性投入** | P1-S1-R6 协作记录第 10 步（含四块接口的源码取证与架构分析） | 延期 |
| SH-P1S1008 | **报表的异步/实时更新可控化**：给所有走 `prepared_report`（后台异步）的报表增加「实时更新」选项——勾选则即时计算，不勾则提供手动更新按钮，且**每次进入报表时提示「当前报表可能不是最新数据」**。本轮实测「库存余额」是唯一 `prepared_report=1` 的报表（物料凭证/会计总账/试算平衡表/损益表/BOM 差异/库存与账务价值对比均为 0），且 `Prepared Report` 表 0 条记录，故点进去恒为空表、无任何提示说明原因 | R3 协作记录第 23 步（用户提出） | 2026-09-20 | **用户数据量大到必须异步处理时**。在此之前的简单处置：把「库存余额」的 `prepared_report` 改为 0（Report 记录上的一个字段，**零代码**）——小厂数据量不需要异步 | P1-S1-R3 协作记录第 23 步 | 延期 |
| SH-P1S1007 | **Link 字段与报表筛选支持拼音检索**：输入 `zangu` 或首字母 `zgkc` 即出现含「暂估库存(已收货，未开票)」的候选列表。当前 Frappe 的 `search_link()` 按 DocType 的 `search_fields` 做 SQL LIKE 子串匹配（`Account.search_fields` 仅 `account_number`、`title_field` 为 None），纯字符匹配做不到拼音。**路径**：Custom Field 存拼音首字母与全拼（保存时以 `pypinyin` 自动生成）+ `override_whitelisted_methods` hook 覆写搜索 query——**不改源码但要写代码，非纯配置** | R3 协作记录第 21 步（用户提出） | 2026-09-20 | 第 ② 阶段（通用版），与翻译改造同批——两者同属"让中国人用得顺"。价值高：中文输入法下打拼音快于切汉字，且科目名很长无人愿打全 | P1-S1-R3 协作记录第 21 步（含 `frappe/desk/search.py:39/214-221/400-401` 取证） | 延期 |

R2 未新增延迟需求条目。R3 新增 SH-P1S1004 ~ 008。**R6 新增 SH-P1S1009**，并给 `SH-P1S1004`、`SH-P1S1006` 各补了内容（见下）。

## Stage 收口时对延迟需求的逐条判定（规范 §12.2）

**本 Stage 只摸底不实现，故 9 条全部"未做"，按规则整批上交本 Phase 概况登记册**（换发 `PH-` 号）。但**优先级与严重性已由 R6 大幅改变**，上交时须带上这层信息：

| 编号 | 做了没有 | 上交时的要点 |
|---|---|---|
| SH-P1S1001 | 未做 | 中国本地化。R5 查明**有一份 unverified 的中国科目表可作素材**，工作量小于从零设计 |
| SH-P1S1002 | 未做 | 计件工资，待确认客户是否行计件制 |
| SH-P1S1003 | 未做 | 客户可读版材料。**R6 使它的时点提前**——客户演示已成最优先需求 |
| **SH-P1S1004** | 未做 | **译名改造。R6 把它从"第 ② 阶段的事"提前为演示前的硬门槛**（用户第 9 步裁决"最优先"）。本轮新增 11 条缺陷、**其中 4 条会导致操作出错**。依据盘点结论见 LG-070：**够立项不够开工**，缺"目标侧术语标准"这个硬前置 |
| **SH-P1S1005** | 未做 | 工时模型与规格参数。**④（Item 规格 Custom Field）仍是性价比最高的二开点**；`SH-P1S1006` 的第五例（树视图空白）与本条的"全量排查"有交集 |
| **SH-P1S1006** | 未做 | 前端不刷新即不显示。**R6 补入第五例并改判一条**：树形 DocType 从侧栏点进恒落列表路由而空白，**原判"偶发、已兜底、不入登记册"被推翻** |
| SH-P1S1007 | 未做 | 拼音检索 |
| SH-P1S1008 | 未做 | 报表异步可控化 |
| **SH-P1S1009** | 未做 | **R6 新增**：外部操控编排机制。**演示前要做，且是长期资产**（MCP 与 React 前端都复用它） |
