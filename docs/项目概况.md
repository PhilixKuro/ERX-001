# ERX-001 项目概况

> 本文件是项目级概况：级联指针（→ 当前 Phase）+ 系统现状 + 延迟需求开放视图。
> 内容边界见 `流程体系/流程规范.md` §9（概况不受常驻文件契约约束）。

## 级联指针

- **当前 Phase**：P1 需求摸底（进行中）→ [01-需求摸底/P1-概况.md](01-需求摸底/P1-概况.md)

## 系统现状

### 项目定位

基于 ERPNext 的二次开发项目。上游 ERPNext 与 Frappe 框架从自有 fork 取得，本项目的功能以 Frappe app 形态叠加在其上。

**目标行业**：弹簧制造（中国大陆中小型厂）。首个潜在客户为弹簧厂，规模与需求待实地考察确认；产品定位为可复用于多家同行业客户，非单客户定制。

**四阶段战略**（P1-S1-R1 讨论记录 DEC-007）：

| 阶段 | 内容 |
|---|---|
| ① 自学 + 摸底 | 先学会用 ERPNext 原生功能，在此过程中摸清原生对弹簧制造业的适用边界与痛点 |
| ② 做中国大陆通用版 | 针对摸出的原生缺口（首要是无中国财税逻辑）改造出适合中国大陆的通用版本 |
| ③ 客户演示 | 以通用版向客户演示 |
| ④ 定制 | 调研客户实际生产环境，定制专属业务逻辑与简单界面 |

后三阶段依次以前一阶段为前置，不并行。

### 技术栈

| 层 | 技术 |
|---|---|
| 框架 | Frappe Framework `version-16`（16.34.0） |
| 业务基座 | ERPNext `version-16`（16.35.0） |
| 后端 | Python 3.14 |
| 前端 | Frappe Desk（DocType 驱动），Node 24 |
| 数据库 | MariaDB 11.8 |
| 缓存 / 队列 | Redis（cache 与 queue 各一实例） |

### 模块结构与职责

| 路径 | 职责 | 形态 |
|---|---|---|
| `docker/` | 容器化开发环境与迁移脚本 | 本仓库自有 |
| `docker/apps.json` | 声明装哪些 app、分支与锁定的 commit | 本仓库自有 |
| `docs/流程体系/` | 跨项目共享的流程规范（SPS） | submodule |
| `frappe-bench/` | bench 本体（venv、站点、构建产物、各 app 源码） | 由 `docker/up.sh` 生成，**整个不进 git** |
| `frappe-bench/apps/frappe/` | Frappe 框架，ERPNext 的依赖 | 独立 git 仓库，origin 指自有 fork |
| `frappe-bench/apps/erpnext/` | ERP 业务基座 | 同上 |
| 自有 app | 本项目全部二次开发内容 | 待创建（将落在 `frappe-bench/apps/` 下） |

遵循 frappe_docker 官方模型：bench 自己 clone 各 app，`frappe-bench/` 不进主仓库版本管理，版本由 `docker/apps.json` 记录（url + branch + commit）。各 app 在其目录内独立 commit、push 到自有 fork。

frappe 与 erpnext 版本须配套，同为 `version-16`。扩展方式与上游同步流程见 `开发守则.md`。

### 开发环境

容器化，宿主机只需 Docker。用法见 [../docker/README.md](../docker/README.md)。

- 站点 `erx.localhost`，web 端口 8000，登录 `Administrator` / `admin`
- 界面语言中文；**demo 数据与 `ERX Demo` 公司已于 P1-S1-R3 清除**，现为公司 `华东弹簧`（缩写 `HDS`），内含一套跑通过完整闭环的弹簧厂数据（主数据 + 采购链 + 生产链 + 销售链），可作后续教材的活样本。科目表与仓库名为**中文**（`存货 - HDS`、`仓库 - HDS` 等）
- **两个备份基准点**（`docker/backups/`）：`20260919_225632` 清场后的干净空站点、`20260921_042056` 闭环已跑通
- 源码在 `frappe-bench/apps/` 下，改动即时生效（bench 直接跑那份代码）
- 数据库与 Redis 用 named volume；换机器靠 `backup.sh` / `restore.sh` 迁移数据，`up.sh` 重建环境

### 能力

待确立（尚未开始功能开发，当前仅有 ERPNext 标准能力与演示数据）。

### 数据模型

沿用 ERPNext 标准 DocType。自有 DocType 与 Custom Field 待确立。

### UI 路由结构

沿用 ERPNext 标准 Desk 路由。自有页面待确立。

### 已知限制 / 技术债

- **`frappe-bench/` 不进版本管理**，故各 app 的源码改动只存在于自有 fork，本仓库仅靠 `docker/apps.json` 记录 commit。换机器前须确认改动已 push，否则会丢。
- **v16 全新安装需额外初始化**：官方把一批初始化搬进了界面配置向导，而 `bench new-site` 会跳过向导。`docker/scripts/seed-demo.sh` 已覆盖（fixtures、默认计量单位、标准价目表、全局币种、配置完成标记），细节见 `docker/README.md`。
- 对 `frappe-bench/apps/erpnext/` 或 `.../frappe/` 的源码改动须登记于此（见 `开发守则.md`）。
- **ERPNext v16 无中国本地化**：`erpnext/regional/` 只有 australia / italy / south_africa / turkey / united_arab_emirates / united_states，**无 china**。增值税专用发票、金税盘对接、银行对账、中国财务报表格式均无原生支持。这是四阶段战略第 ② 阶段（做中国大陆通用版）的主要工作面。
  - **但科目表模板另有一份中国版，不在 `regional/`**（P1-S1-R5 实测，修正此前"完全无中国科目表"的表述）：`accounts/doctype/account/chart_of_accounts/unverified/cn_l10n_chart_china.json` 存在，名为 `China - 中国会计科目表（财会[2006]3号《企业会计准则》）`，含 82 个一级节点（原材料 / 生产成本 / 库存商品 / 委托加工物资 / 应交税费下 11 个税种等）。**可作第 ② 阶段的素材，但不能直接用**，三个原因：① 它在 `unverified/` 而 `get_charts_for_country()` 默认只扫 `verified/`（`chart_of_accounts.py:147-149`），故建公司时下拉里选不到（`verified/` 下 78 个模板无一以 `cn` 开头，中国用户只有 `Standard` 与 `Standard with Numbers` 两项）；② **82 个节点全部平铺、`root_type` 全为空**，导进来是 82 棵无 `root_type` 的孤树而非挂在资产/负债/权益/收入/费用五根下的树，导入靠 `ignore_mandatory` 绕过校验、进库后报表归类失效；③ **内容停在 2006 准则未更新**——含营改增（2016）后已废止的「应交营业税」与「营业税金及附加」，且「应交增值税」下无进项/销项二级明细。**故第 ② 阶段的工作是"补全一份现成清单"（补 `root_type`/`account_type`/`account_number`、按五大类重组、清理废止科目）而非"从零设计"，但仍须懂中国准则的会计参与。** 详见 [L0 教材](01-需求摸底/S01-场景摸底/0-P1-S1文档/L0-记账底座与组织骨架.md) 段④缺口一。
- **hrms app 未安装**：当前 `frappe-bench/apps/` 只有 frappe 与 erpnext。薪资相关能力（含计件工资）无 app 支撑。
- **制造与销售模块无优先级字段**：`manufacturing` / `selling` / `stock` 三模块内仅 Putaway Rule 有 `priority`，Work Order 与 Job Card 均无。排产为朴素顺排（按工序工时累加 + 冲突顺延），且无换型/调机时间概念（无 `setup_time` / `changeover` 字段，仅有全局固定的 `mins_between_operations`）。详见 P1-S1-R1 讨论记录。
- **生产计划不按工艺损耗反算投料量**（P1-S1-R3 实测）：`production_plan.py` 里 `process_loss` 无任何引用，`planned_qty` 直接取 SO 待交数量，故每张按销售订单生成的工单必然短交（SO 5000 支 + BOM 5% 损耗 → 完工 4750 支）。MRP 的 scrap factor / yield factor 缺失，SAP 与 Oracle 均有该能力。真实代价是每张订单都要补做一次，流程成本翻倍。
- **工序成本按实际报工工时计，故成本准确性完全依赖车间真实报工**（P1-S1-R3 实测）：任务单计时几秒即导致工序成本记 0.27 元而非应有的 433.3 元，毛利率虚高 40 个百分点。
- **完工入库的额外费用科目须逐行手填**（P1-S1-R3 实测）：公司的 **`expenses_added_to_stock_account`**（Company「库存费用」节）为 None，故每张完工入库单要手填 N 行费用科目（N = 工序数 × 成本构成数），填错（如填成 `销货成本`）会让成本进损益表而非库存资产、成本核算直接错。须设该字段默认值为 `结转库存的费用 - HDS`。**注**：该字段名曾误记为 `expenses_included_in_valuation`（v13/v14 旧名，在 v16 源码零命中），照旧名找不到该字段；`Expenses Included In Valuation` 在 v16 仅作 **account_type 值**存在。见 P1-S1-R3 忠实度报告 FD-07。
- **前端多处改动后不刷新即不显示**（P1-S1-R3 累积六例）：仓库列表空白、工作区侧栏卡片缺失、BOM 保存后「提交」按钮不出现、`Job Card` 的 `time_logs` 残留空行致计时器状态错乱等。**第 ③ 阶段客户演示前必须闭环**，详见 P1-S1 概况登记册 `SH-P1S1006`。

## 延迟需求开放视图

（汇总各 Phase 概况登记册中仍开放的延迟需求，规则见 `流程体系/流程规范.md` §12.1）

暂无。
