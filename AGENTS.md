# ERX-001 — Agent 指引

本项目的流程体系以 git submodule 形式挂载在 `docs/流程体系/`（SPS - Shared Process Spec）。
本文件与 `CLAUDE.md` 保持一致的流程触发规则，确保 Agent 执行时也遵循流程规范。

## 流程中枢触发规则（开发流程入口）

- 消息意图是"推进本项目开发"（继续开发 / 加改功能 / 规划 / 构建 / 审查 / 确认，
  或开发语境下说"继续"），或用户指名某条 Workflow，或用户要"使用议题"/"展开为议题"/"分解为子 Stage" → 先载入
  `docs/流程体系/流程中枢.md`，按其内容执行。
- 一般知识问答、解释、闲聊、任何不以改动或推进本项目为目的的消息 → 不载入，直接回答。
- 拿不准 → 不自动触发（直接答或先反问）。误触发比漏触发代价高。

## 流程讨论记录规则

（同 CLAUDE.md）

涉及流程、开发守则、流程规范、工作方式、协作方法的讨论，必须先载入 `docs/流程体系/讨论契约.md`，按其纪律呈现讨论并即时记录到讨论记录文件，每轮回答后立即追加。若讨论要改动流程体系文件，另载入 `docs/流程体系/流程改进指南.md`。

## 常驻文件写入规则

（同 CLAUDE.md）

往 CLAUDE.md / AGENTS.md / docs/README.md / docs/项目概况.md / docs/开发守则.md / docs/业务规则.md 写内容前，先载入 `docs/流程体系/常驻文件契约.md`。

## SPS 修改前置规则

（同 CLAUDE.md）

修改 `docs/流程体系/` 内任何文件前：`cd docs/流程体系` → `git pull origin main` → 确认无冲突 → 再修改 → 尽快推送。
