# ERX-001

本项目的流程体系以 git submodule 形式挂载在 `docs/流程体系/`（SPS - Shared Process Spec）。

## 流程中枢触发规则（开发流程入口）

- 消息意图是"推进本项目开发"（继续开发 / 加改功能 / 规划 / 构建 / 审查 / 确认，
  或开发语境下说"继续"），或用户指名某条 Workflow，或用户要"使用议题"/"展开为议题"/"分解为子 Stage" → 先载入
  `docs/流程体系/流程中枢.md`，按其内容执行。
- 一般知识问答、解释、闲聊、任何不以改动或推进本项目为目的的消息 → 不载入，直接回答。
- 拿不准 → 不自动触发（直接答或先反问）。误触发比漏触发代价高。

## 流程讨论记录规则

涉及流程、开发守则、流程规范、工作方式、协作方法的讨论（用户消息中出现这些关键词或语义），必须**先载入 `docs/流程体系/讨论契约.md`**，然后按其纪律呈现讨论、并即时记录到讨论记录文件（路径见 docs/README.md），每轮回答后立即追加，不允许等讨论结束再补记。不需要用户提醒。不要等全部讨论完再记录。

若该讨论要**改动**流程体系文件（规范 / Workflow / Spec / play / 契约），另载入 `docs/流程体系/流程改进指南.md`，以其内容为全部指令。

## 常驻文件写入规则

往 CLAUDE.md / AGENTS.md / docs/README.md / docs/项目概况.md / docs/开发守则.md / docs/业务规则.md 写内容前，先载入 `docs/流程体系/常驻文件契约.md`，按其判据决定写哪个文件、写什么、以及顺带清掉哪些已失效内容。不需要用户提醒。

## 版本管理操作规则

**默认不做任何"落进版本控制"的操作。** 两个例外：① 用户在对话里显式允许；② 流程步骤的 `打tag` 字段要求。

须显式许可的操作按本项目实际用的版本控制填——用 git 的填 `commit` / `push` / `tag` / `reset` / `restore` / `checkout` / 改分支；若另有其它版本控制（如 Perforce），把它的落库操作一并列进来。

**不受限**：只读查询，以及只改本地可写状态、可完全撤销的操作。

**准备与落地须分开报**：可以把改动写好、描述拟好，但"是否提交"须停下来问。**用户裁决"改某条规则"不等于裁决"提交它"。**

> 本节是 `流程规范.md` §4.1「Git 动作」列所指向的那一节，**必须存在于消费方 CLAUDE.md**——流程改进类会话不经任何 Workflow，只有落在 CLAUDE.md 才管得住（见讨论记录-8#第553步）。

## SPS 修改前置规则（Submodule 同步）

修改 `docs/流程体系/` 内任何文件前，必须：

1. **进入 SPS submodule 目录**：
   ```bash
   cd docs/流程体系
   ```

2. **拉取最新提交**：
   ```bash
   git pull origin main
   ```
   
3. **检查是否有冲突**：
   - 如果本地有未提交改动，`git pull` 会失败
   - 此时先 stash 或 commit 本地改动，再 pull

4. **确认拉取成功后再开始修改**

5. **改完即请许可推送**：修改完成后**问用户是否可以提交并推送**，获准后立即推，以减少其它项目的冲突窗口。**本条不是自行推送的授权**——见「版本管理操作规则」

**违规后果**：
- 推送时可能被拒绝（remote 有更新的 commit）
- 讨论记录步号冲突
- 改进登记册编号重复
